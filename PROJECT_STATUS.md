# حالة المشروع — عيادة د. خليل

آخر تحديث: 2026-07-28 (جلسة تصحيح محتوى الموقع العام — Content Accuracy Update)
هذا الملف هو المرجع الوحيد المُحدَّث لحالة المشروع الفعلية. يُقرأ بدل إعادة تحليل الكود من الصفر في كل جلسة جديدة. راجع أيضًا `NEXT_TASK.md` لمعرفة نقطة التوقف الدقيقة والخطوة التالية الموصى بها.

---

## ✅ فحص جاهزية الإنتاج (Production Readiness Check) — 2026-07-28

فحص سريع بطلب صريح من المستخدم: **لا ميزات جديدة، لا إعادة تصميم** — فقط تحقّق وإصلاح ما هو حرج.

- **`npm run lint`**: ✅ صفر أخطاء (تحذير واحد فقط، معروف ومقبول مسبقًا: `react-hooks/incompatible-library` على `watch()` من `react-hook-form` في `appointment-form.tsx` — تحذير غير حاجز، لا علاقة له بأي تعديل حديث).
- **`npm run build`** (بعد حذف `.next` للتأكد من بناء نظيف): ✅ نجاح تام، صفر أخطاء TypeScript، صفر تحذيرات بناء. جدول المسارات النهائي مطابق للمتوقَّع (`/[lang]`, `/[lang]/appointment` للموقع العام، كل مسارات `/admin/*` و`/login` كما هي).
- **لم يُكتشف أي خطأ TypeScript أو ESLint يحتاج إصلاحًا** — الكود كان نظيفًا بالفعل من الجلسة السابقة، لم يكن هناك ما يستدعي تعديلًا.
- **تحقّق المسارات عبر HTTP على بناء إنتاج حقيقي** (`next build` + `next start`، وليس `next dev`): `/`, `/appointment`, `/en`, `/en/appointment`, `/login`, `/favicon.ico` ← **200**، `/admin` لزائر مجهول ← **307**. **6/6 نجحت**.
- **المصادقة**: حساب طبيب تجريبي حقيقي (Supabase Auth + Prisma) — `/admin` ← **200** بعد تسجيل الدخول، `/login` ← **307** (تحويل تلقائي لمستخدم مسجّل دخول بالفعل)، `/admin` لزائر مجهول ← **307**. **3/3 نجحت**.
- **المرضى/الفوترة/التقارير/إعدادات العيادة**: `/admin/patients`, `/admin/billing`, `/admin/reports`, `/admin/settings/clinic` (+ `/admin/appointments`, `/admin/staff`, `/admin/doctors`, `/admin/activity`, `/admin/notifications` للاطمئنان) ← **200** جميعها لحساب الطبيب، ومحتوى صفحة المرضى يحتوي فعليًا على نصوص "المرضى"/"مريض" (وليس افتراضًا أن 200 يعني محتوى صحيح). **10/10 نجحت**.
- **الحجز (عربي + إنجليزي)**: حجز عربي حقيقي كامل نجح فعليًا (مريض + موعد أُنشئا في قاعدة البيانات، تم التحقق مباشرة ثم حُذفا)، ومحاولة حجز إنجليزية في يوم مغلق (جمعة 2026-08-07) أعادت فعليًا رسالة تحقق **إنجليزية** صحيحة تتضمّن "Friday". **4/4 نجحت**.
- **الموقع الإنجليزي**: `/en` يحتوي فعليًا نص "Book Your Appointment" ووسوم `lang="en" dir="ltr"`، `/en/appointment` ← 200 — تم التحقق من HTML الفعلي المُستلَم وليس افتراضًا. **الموقع العربي**: `/` ما زال `lang="ar" dir="rtl"` — لا رجوع عن أي شيء.
- **مجموع فحوصات HTTP/منطق فعلية هذه الجولة: 23/23 نجحت**، بالإضافة لـ`lint`/`build` الناجحين. تم حذف الحساب التجريبي وكل بيانات الحجز التجريبية بالكامل بعد الانتهاء (لا أثر متبقٍ في قاعدة البيانات).
- **⚠️ ملاحظة غير متعلقة بالكود، لوحظت في سجل خادم الإنتاج أثناء الفحص**: رسالتا خطأ `Failed to find Server Action "<id>". This request might be from an older or newer deployment.` ظهرتا فور إقلاع خادم الإنتاج، قبل أي طلب فحص فعلي. هذا **سلوك متوقَّع وغير حاجز** لـ Next.js: أي تبويب متصفح مفتوح من قبل (من بناء/جلسة سابقة) يحتفظ بمعرّفات Server Actions قديمة، وتتغيّر هذه المعرّفات مع كل `build` جديد — الخادم يرفضها بأمان بدل تنفيذ إجراء خاطئ. **ليس خطأ في الكود، ولا يحتاج إصلاحًا**؛ يختفي تلقائيًا بإعادة تحميل الصفحة في المتصفح المعني.
- **لم يُختبر بصريًا في متصفح حقيقي** (لا أداة أتمتة متصفح متاحة هذه الجلسة أيضًا) — نفس حد الاختبار المُتَّبع في كل الجلسات السابقة لهذا المشروع.
- **الخلاصة: لا مشاكل حرجة، لا حاجة لأي إصلاح.** خادم التطوير (`next dev`) أُعيد تشغيله في نهاية الفحص (كان يعمل بطلب المستخدم قبل هذه الجلسة) ليبقى متاحًا كما كان.

---

## ✅ تصحيح محتوى الموقع العام (Public Website Content Accuracy) — 2026-07-28

طلب صريح من المستخدم: **الطبيب خريج حديث**، لا يملك 10+ سنوات خبرة ولا آلاف الحالات الناجحة — يجب ألا يوحي أي نص في الموقع العام بعكس ذلك. **لا إعادة تصميم، لا تعديل وظيفي، تحديث نصوص فقط.**

- **اسم الطبيب/العيادة صار "د. خليل" / "عيادة د. خليل"** (بلا "الجمعة") في كل مكان: `ClinicSettings.doctorName`/`clinicName` (تصحيح بيانات مباشر في قاعدة البيانات — نفس الحقول التي يُعدِّلها المدير عبر `/admin/settings/clinic` عادةً، لم تُمس شاشة/كود الإعدادات نفسها) + `DEFAULT_CLINIC_SETTINGS` (القيمة الافتراضية الاحتياطية في الكود) + `siteConfig` غير المُستخدَم فعليًا (تصحيح للاتساق فقط). **`doctorTitle` ("طبيب أسنان عام" بدل "استشاري طب وتجميل الأسنان")** — اكتُشف أثناء الفحص أن هذا الحقل **ليس عمودًا في قاعدة البيانات إطلاقًا** (`getClinicSettings()` يُثبِّته دائمًا من `DEFAULT_CLINIC_SETTINGS.doctorTitle` بغض النظر عن أي شيء)، فالتعديل الوحيد الممكن/الصحيح كان في الكود مباشرة. **لم يُمَس** سجل `Doctor` الإداري (`firstName`/`lastName`/`specialty`، المُستخدَم في `/admin/doctors` والمواعيد والفواتير) — بيانات تشغيلية منفصلة تمامًا عن هوية العيادة المعروضة على الموقع العام، محمية صراحة تحت "لا تعديل على لوحة الإدارة".
- **قسم "عن الدكتور"**: استُبدلت السيرة بالكامل بالنص المطلوب حرفيًا من المستخدم (+ ترجمة إنجليزية مطابقة). أُضيفت **المهمة والرؤية** كفقرتين ضمن نفس القسم (بلا قسم/مكوّن جديد — التزامًا بـ"لا إعادة تصميم")، و**القيم الخمس** (الجودة، الصدق، راحة المريض، الاحترافية، التعلّم المستمر) كقائمة صغيرة أسفل بطاقات المؤهلات. المؤهلات الأربع (نفس البنية البصرية 2×2، دالة بيانات فقط) أُعيد صياغتها لتناسب خريجًا حديثًا: بقيت شهادة البكالوريوس فقط كما هي (حقيقة أساسية لأي طبيب أسنان)، حُذفت "دبلوم تجميل من معهد أوروبي" و"عضوية نقابة منذ 2010" (تُوحي بسنوات تراكمية من التدريب/الخبرة)، واستُبدلتا بـ"التزام بالتطور المهني المستمر" و"عضوية نقابة مسجّلة" بلا تاريخ. الشارة العائمة على الصورة ("+15 سنة / خبرة موثوقة") استُبدلت بـ"التزام بالجودة / في كل خطوة" (نفس مكان/تصميم الشارة، محتوى فقط تغيّر).
- **إحصائيات وهمية أُزيلت بالكامل**: شبكة الثلاث إحصائيات في Hero ("15+ سنة خبرة"، "8000+ مريض راضٍ"، "4.9 تقييم") **حُذفت من الكود فعليًا** (وليس إخفاؤها فقط) — الخيار الثاني الذي صرَّح به المستخدم صراحةً ضمن "استبدل بمعلومة محايدة أو احذف القسم بالكامل"، اختير الحذف لتفادي اختلاق أرقام "محايدة" لا معنى لها في شكل إحصائية. البطاقة العائمة الأخرى التي كانت تعرض "4.9 / 5 — تقييم المرضى" (بجانب أيقونة نجمة) استُبدلت بمحتوى غير رقمي وصادق ("أحدث الأساليب / رعاية حديثة") مع تبديل الأيقونة من نجمة إلى Sparkles لتفادي إيحاء تقييم مزيّف. "خبرة تفوق 15 عامًا" و"ثقة آلاف المرضى" في قسم "لماذا نحن" استُبدلتا أيضًا بـ"نهج علمي حديث" و"تواصل صادق وواضح".
- **آراء المرضى الوهمية أُزيلت بالكامل** (3 تقييمات مُختلَقة بأسماء وتفاصيل خيالية) واستُبدلت بحالة "لا نتائج بعد" مع رسالة توضيحية: "ستظهر آراء المرضى الحقيقية هنا بعد الحصول على موافقتهم." — تطلَّب هذا تعديلًا صغيرًا في `testimonials.tsx` (شرط عرض بديل عند فراغ القائمة بدل شبكة البطاقات الثلاث، مع إزالة تقييم الخمس نجوم المزيّف من هذه الحالة تحديدًا) — أصغر تغيير كود ضروري لتحقيق طلب "إزالة تقييمات وهمية" فعليًا بدل تركها فارغة بصريًا بشكل مكسور.
- **الخدمات لم تُمس** (طُلب إبقاؤها كما هي) — لا وصف فيها يوحي بسنوات خبرة أصلًا، فلم تحتج تعديلًا.
- **SEO**: عنوان/وصف الصفحة (`generateMetadata`) يقرآن `clinicName`/`clinicDescription` من `ClinicSettings` مباشرة — انعكس التصحيح تلقائيًا بصفر تعديل إضافي على كود الـ metadata نفسه.
- **الموقع الإنجليزي**: كل تعديل أعلاه طُبِّق بالتوازي في `en.ts` بترجمة مطابقة (وليس ترجمة آلية لاحقة) — النوع `Dictionary` المُشتَق من `ar.ts` يضمن عدم نسيان أي مفتاح.
- **`npx tsc --noEmit`, `npx eslint src`, `npm run build`**: نجاح تام بدون أخطاء بعد كل التعديلات.
- **تحقّق فعلي عبر HTTP على بناء إنتاج حقيقي** (`next build` + `next start`): تأكيد ظهور "د. خليل"/"Dr. Khalil" في الصفحتين، غياب تام لأي رقم إحصائي وهمي (تحقّق نصي مباشر في HTML، الاستثناء الوحيد الذي ظهر في البحث كان إحداثيات مسار SVG لا علاقة لها بالمحتوى)، ظهور نص المهمة/الرؤية بالضبط في الصفحتين، ظهور رسالة "ستظهر آراء المرضى الحقيقية..."/"Real patient reviews will appear here..." بدل التقييمات المزيّفة، غياب "استشاري" و"الجمعة"/"Al Jumaa" تمامًا من الصفحتين (المطابقة الوحيدة المتبقية لكلمة "الجمعة" هي معناها الآخر "يوم الجمعة" في رسائل إغلاق العيادة، غير ذات صلة). صفحة `/admin/settings/clinic` بحساب طبيب تجريبي حقيقي ← **200**، تعرض القيم المصحَّحة بشكل صحيح (تأكيد أن شاشة الإعدادات نفسها لم تُكسَر). `/admin` لزائر مجهول ← **307** كالمعتاد (لا كسر في لوحة الإدارة).
- تم حذف الحساب التجريبي بالكامل بعد الانتهاء؛ لا بيانات اختبار متبقية.
- **لم يُختبر بصريًا في متصفح حقيقي** (نفس القيد المتكرر — لا أداة أتمتة متصفح متاحة). التحقق اعتمد على HTML الفعلي المُستلَم عبر HTTP + نجاح `tsc`/`eslint`/`build`، بنفس منهجية كل الجلسات السابقة.

---

## ✅ الوحدات المكتملة (Completed Modules)

| # | الوحدة | الحالة | ملاحظة |
|---|---|---|---|
| 0 | مرحلة التثبيت (Stabilization) | ✅ مكتملة | أساس المشروع: Next.js + Supabase + Prisma، هوية بصرية Minimal Luxury |
| 1 | الموقع العام (Public Site) | ✅ مكتملة | Hero، الخدمات، الحجز، RTL كامل، Framer Motion |
| 2 | نظام حجز المواعيد | ✅ مكتملة | Modal + Zod + منع تعارض + حفظ ذرّي في Supabase |
| 3 | لوحة الإدارة + المصادقة (RBAC) | ✅ مكتملة | Supabase Auth حقيقي، حماية proxy.ts + كل صفحة |
| 4 | السجل الطبي الإلكتروني (EDR) | ✅ مكتملة | ملف مريض كامل، Odontogram FDI، زيارات سريرية، ملفات Storage |
| 5 | إدارة العلاج والفوترة (Treatment & Billing) | ✅ مكتملة | خطط علاج، فواتير، دفعات، تقارير |
| 6 | إعدادات العيادة (White-label Foundation) | ✅ مكتملة | كل هوية العيادة قابلة للتعديل من `/admin/settings/clinic` |
| 7 | إدارة الموظفين (Staff Management) | ✅ مكتملة | 6 أدوار، صفحات كاملة، صور شخصية، إعادة تعيين كلمة مرور |
| 8 | تقويم المواعيد المتقدم (Advanced Appointment Calendar) | ✅ مكتملة | عرض يوم/أسبوع/شهر، سحب وإفلات، تغيير المدة، ألوان أطباء |
| 9 | مركز الإشعارات (Notifications Center) | ✅ مكتملة | 10 أنواع أحداث تلقائية، Realtime حقيقي + Polling احتياطي، صلاحيات لكل دور |
| 10 | إدارة الأطباء (Doctor Management) | ✅ مكتملة | `/admin/doctors`، إضافة/تعديل/تفعيل/تعطيل، بصلاحية `manageStaff` نفسها |
| 11 | التقارير والتحليلات (Reports & Analytics) | ✅ مكتملة | `/admin/reports` أُعيد بناؤها بالكامل: 10 بطاقات، 8 رسوم بيانية، فلاتر (فترة + طبيب)، تصدير CSV/Excel/PDF |
| 12 | البحث الشامل الذكي (Smart Global Search) | ✅ مكتملة | لوحة أوامر Ctrl+K، 8 فئات بحث، صلاحيات لكل فئة، تمييز نص مطابق، تنقّل بلوحة المفاتيح، بحث أخير |
| 13 | صلاحيات الأدوار الأربعة الجديدة + حماية صفحات المرضى/المواعيد | ✅ مكتملة | `ASSISTANT`/`HYGIENIST`: اطّلاع سريري فقط. `ACCOUNTANT`: فوترة وتقارير فقط. `ADMIN`: بلا تغيير (`manageStaff` فقط). `/admin/patients`/`/admin/appointments` الآن تُحوِّل (307) لمن لا يملك الصلاحية |
| 14 | **الموقع العام ثنائي اللغة (عربي/إنجليزي)** | ✅ مكتملة **(آخر ما أُنجز)** | لوحة الإدارة بقيت عربية بالكامل بلا أي تغيير؛ الموقع العام + صفحة الحجز فقط أصبحا ثنائيي اللغة، `/en` بادئة اختيارية، تبديل لغة + تذكّر الاختيار، RTL/LTR تلقائي، SEO لكل لغة |

### تفاصيل كل وحدة

**الموقع العام + الحجز**: صفحة رئيسية كاملة الأقسام (Hero/الخدمات/عن الطبيب/لماذا نحن/التواصل)، حجز عبر Modal مع تحقق Zod ومنع تعارض المواعيد.

**لوحة الإدارة**: `/admin` (لوحة تحكم)، `/admin/appointments`، `/admin/patients`، `/admin/billing`، `/admin/reports`، `/admin/staff`، `/admin/activity`، `/admin/settings/clinic` — جميعها ببيانات حقيقية من قاعدة البيانات ومزامنة فورية.

**EDR** (`/admin/patients/[id]`): معلومات شخصية مع احتساب العمر تلقائيًا، سجل طبي (حساسية/أمراض مزمنة/أدوية/تدخين/حمل/فصيلة دم)، Odontogram تفاعلي (32 سنًا، ترقيم FDI، 12 حالة)، سجل زيارات سريرية، ملفات (أشعة/صور/PDF) عبر Supabase Storage bucket خاص `patient-files`، بحث شامل.

**العلاج والفوترة**: خطط علاج متعددة لكل مريض (عنوان/تشخيص/أولوية/حالة/تواريخ)، إجراءات علاج لكل خطة (إجمالي محسوب تلقائيًا)، مخطط زمني يدمج الزيارات والإجراءات، فواتير برقم تسلسلي `INV-YYYY-NNNN` مع بنود ثابتة (snapshot) وقت الفوترة، دفعات متعددة لكل فاتورة، طباعة/تصدير PDF عبر `@media print` (بدون مكتبة خادم)، تقارير إيرادات ومرضى نشطين.

**إعدادات العيادة** (`/admin/settings/clinic`): 6 أقسام (عام، هوية بصرية + ألوان، تواصل، عنوان، ساعات دوام، وسائل تواصل اجتماعي) — كل بيانات هوية العيادة أصبحت من قاعدة البيانات بدل الكود، مع قيمة افتراضية تطابق التصميم الأصلي حرفيًا إن لم تُضبط الإعدادات (لا تغيير بصري تلقائي).

**إدارة الموظفين** (`/admin/staff`, `/admin/staff/new`, `/admin/staff/[id]`): 6 أدوار (`DOCTOR`, `RECEPTIONIST`, `ASSISTANT`, `HYGIENIST`, `ACCOUNTANT`, `ADMIN`)، حقول كاملة (اسم/بريد/هاتف/صورة شخصية/مسمى وظيفي/ملاحظات/تاريخ تعيين/حالة)، إعادة تعيين كلمة مرور إدارية، تفعيل/تعطيل، حذف، تعديل بريد إلكتروني مع مزامنة Supabase Auth. تفاصيل الصلاحيات في قسم "قيود معروفة" أدناه.

**تقويم المواعيد المتقدم** (`/admin/appointments`، الوضع الافتراضي الجديد): استبدل القائمة كواجهة افتراضية، مع الإبقاء على القائمة القديمة كاملة وبلا أي تغيير خلف زر "عرض القائمة" (`?mode=list`) — لم يُحذف أو يُعدَّل أي كود في مسار القائمة.
- **3 عروض**: يوم/أسبوع/شهر، مع تنقّل (اليوم/التالي/السابق/انتقال لتاريخ محدد) عبر searchParams في الرابط (`view`, `date`, `doctorId`, `mode`) — يدعم الرجوع والمشاركة كرابط مباشر.
- **الأحداث ملوّنة حسب الحالة** (6 حالات الآن، أُضيفت `NO_SHOW` "لم يحضر" كحالة جديدة — الحالات الخمس السابقة NEW/PENDING/CONFIRMED/COMPLETED/CANCELLED لم تتغيّر).
- **السحب والإفلات + تغيير المدة**: منفَّذان بـ HTML5 Drag and Drop API الأصلي وأحداث Pointer يدوية للتمديد (بدون أي مكتبة خارجية جديدة — لا `dnd-kit` ولا `react-dnd` ولا `FullCalendar`)، مع فحص تعارض حقيقي (`rescheduleAppointment` الجديد) وفحص ساعات الدوام قبل الحفظ.
- **النقر على موعد** يفتح نافذة تفاصيل (حالة، تأكيد/إكمال/إلغاء/لم يحضر، تعديل، حذف) تُعيد استخدام نفس Server Actions الموجودة مسبقًا (`updateAppointmentStatus`, `deleteAppointment`) ونفس `EditAppointmentDialog`.
- **النقر المزدوج على خانة فارغة** يفتح نافذة "حجز جديد" (نفس `NewAppointmentDialog` الموجودة، بحقول اختيارية جديدة `initialDate/initialTime/initialDoctorId` — غير مُلزمة، فلا يتأثر أي استدعاء قديم لها) مع الوقت والطبيب مُعبَّأين مسبقًا.
- **ساعات الدوام تُقرأ فعليًا من `ClinicSettings.workingHours`**: الأيام المغلقة تُعرض مظللة، والساعات خارج الدوام تُمنع من السحب/الإفلات/الإنشاء في هذا التقويم تحديدًا (خادميًا وواجهيًا) — دون المساس بمنطق الحجز العام الحالي في `appointment-slots.ts` (بقي كما هو تمامًا، غير مربوط بعد بالإعدادات، كما كان موثَّقًا سابقًا).
- **تعدد الأطباء**: كل طبيب بلون مختلف (من نفس لوحة الرسوم البيانية الحالية `--chart-1`..`--chart-5`)، وفلترة حسب الطبيب من شريط الأدوات — يعمل تلقائيًا إن وُجد أكثر من طبيب واحد في القاعدة. **تحديث لاحق**: واجهة إضافة/تعديل/تعطيل الأطباء أُضيفت في جولة لاحقة (`/admin/doctors`، راجع قسم "إدارة الأطباء" أدناه)؛ فلتر التقويم يعرض الأطباء الفعّالين فقط الآن.
- **الشريط الجانبي**: مواعيد اليوم، المواعيد القادمة، قائمة الانتظار (المواعيد بحالة "جديد/قيد المراجعة") — بيانات حقيقية من نفس جدول `Appointment`.
- **الأداء**: كل عرض يجلب فقط مواعيد نطاقه الزمني المعروض (يوم/أسبوع/6 أسابيع للشهر)، وعرض الشهر يحدّ كل خانة يوم بـ3 أحداث ظاهرة + "+N أخرى".

**مركز الإشعارات** (`/admin/notifications` + جرس الإشعارات في شريط لوحة الإدارة): نظام إشعارات داخل التطبيق كامل، مبني فوق نموذج `Notification` جديد وحيد (لا تكرار مع `ActivityLog` الموجود — الاثنان يخدمان غرضين مختلفين تمامًا، راجع التفصيل أدناه).

- **10 أحداث تُولِّد إشعارًا تلقائيًا**: حجز جديد (من الموقع العام وأيضًا من لوحة الإدارة)، إلغاء موعد، تعديل/نقل موعد، اكتمال موعد، مريض جديد (من أي من مسارات إنشاء المريض الثلاثة)، فاتورة جديدة، دفعة مستلمة، خطة علاج جديدة، موظف جديد، تحديث إعدادات العيادة. كل حدث يُنشئ الإشعار فور نجاح العملية الأصلية عبر استدعاء إضافي (`createNotification`) بجانب `logActivity` الموجود مسبقًا — **لم يتغيّر أي منطق عمل موجود**، فقط إضافة استدعاء واحد إلى كل مسار.
- **4 مستويات أولوية**: منخفضة/متوسطة/عالية/حرجة — معيَّنة يدويًا لكل نوع حدث حسب أهميته التشغيلية (مثال: إلغاء موعد = عالية، موظف جديد = عالية لأنه أمر حساس أمنيًا، اكتمال موعد = منخفضة). لا حدث حالي يُصنَّف "حرجة" تلقائيًا — المستوى موجود ومدعوم بالكامل في الواجهة والفلاتر لاستخدام مستقبلي.
- **Realtime حقيقي — تم التحقق فعليًا وليس افتراضًا**: تم أولًا التأكد تجريبيًا أن Supabase Realtime مُفعَّل فعليًا على هذا المشروع (نشر `supabase_realtime` موجود)، ثم أُضيف جدول `Notification` إليه + سياسة RLS للقراءة (`SELECT` لمستخدمي `authenticated` فقط، لا تؤثر على قراءة/كتابة Prisma لأنه يتصل كمالك الجدول). تم اختبار استقبال حدث Realtime حقيقي (اشتراك فعلي + إدراج صف + استقبال الحدث خلال أقل من ثانية) عبر سكربت مستقل قبل اعتماد الميزة.
- **Polling احتياطي دائم كل 30 ثانية بجانب Realtime** (وليس بديلًا عنه) — يعمل الاثنان معًا: أي حدث يصل عبر Realtime يُحدِّث الواجهة فورًا، والـ Polling شبكة أمان ذاتية الإصلاح إن انقطع الاتصال أو تأخّر. لا حاجة لاكتشاف "هل Realtime متاح؟" برمجيًا — التصميم يعمل بشكل صحيح في الحالتين تلقائيًا.
- **الصلاحيات** (`src/lib/notifications.ts`, `getVisibleNotificationTypes`): **الطبيب** يرى كل الإشعارات. **موظف الاستقبال** يرى فقط إشعارات المواعيد والمرضى (قائمة صريحة مطابقة حرفيًا للطلب — وليس عبر نظام الصلاحيات العام، لأن موظف الاستقبال يملك فعليًا `viewBilling`/`viewMedicalRecord` في باقي التطبيق لكن يجب ألا يرى إشعارات الفواتير/العلاج). **بقية الأدوار** (مدير النظام/مساعد طبي/أخصائي نظافة/محاسب) ترى فقط الإشعارات التي تملك الصلاحية المرتبطة بها عبر نظام الصلاحيات العام — وبما أن هذه الأدوار محدودة الصلاحيات عمدًا حاليًا (راجع قيود إدارة الموظفين)، فإنها ترى عددًا قليلًا جدًا أو صفر إشعارات إلى أن تُحدَّد صلاحياتها الفعلية.
- **الميزات**: تحديد كمقروء (فردي)، تحديد الكل كمقروء (**محدود بنطاق رؤية الدور** — لا يمكن لموظف استقبال أن "يقرأ" إشعارًا لا يراه أصلًا، تفصيل التصميم في القيود أدناه)، حذف، فلترة حسب النوع (القائمة المعروضة تُبنى ديناميكيًا من الأنواع المرئية لدور المستخدم فقط)، فلترة غير المقروء فقط، بحث نصي في العنوان/الرسالة.
- **جرس الإشعارات في شريط لوحة الإدارة**: استبدل الجرس القديم (الذي كان يعرض فقط عدد "مواعيد لم تُشاهَد بعد" ويُصفِّرها بالنقر) بجرس حقيقي: عداد غير مقروء مُصفَّى حسب الدور، قائمة منسدلة بأحدث 8 إشعارات، تحديد الكل كمقروء، رابط لمركز الإشعارات الكامل. الحقل القديم (`Appointment.seenAt`) والدوال المرتبطة به لم تُحذف من الكود (لم تكن مستخدمة في أي مكان آخر، تم التحقق) لكنها لم تعد مستخدمة من الجرس.
- **قاعدة البيانات**: نموذج واحد جديد فقط (`Notification`: نوع، أولوية، عنوان، رسالة، رابط اختياري، مقروء/غير مقروء، تاريخ) — **لم يُكرَّر** أي شيء موجود في `ActivityLog` رغم أن نقاط الاستدعاء متشابهة؛ `ActivityLog` سجل تدقيق دائم لا يُحذف أبدًا ويُبنى لغرض مختلف تمامًا (لا حالة قراءة، لا صلاحيات دور، لا أولوية)، بينما `Notification` قابل للحذف والتحديث تفاعليًا من قِبَل المستخدم — دمجهما في جدول واحد كان سيكسر صفحة "سجل النشاط" الموجودة (حذف إشعار كان سيحذف سجل تدقيق دائم). حالة "مقروء" في `Notification` **إشعار واحد لكل الفريق وليست لكل مستخدم على حدة** (حقل `isRead` عام وليس جدول ربط لكل مستخدم) — قرار مبسَّط مقصود ("الحد الأدنى من المخطط" كما طُلب) مناسب لفريق عيادة صغير، راجع القيد أدناه.

**إدارة الأطباء** (`/admin/doctors`, `/admin/doctors/new`, `/admin/doctors/[id]`): أُغلقت الفجوة الموثَّقة منذ جولة التقويم — أول واجهة فعلية لإضافة/تعديل/تفعيل/تعطيل طبيب، بدل التلاعب المباشر بقاعدة البيانات.
- **أسئلة التصميم أُجيبت صراحة من المستخدم قبل البناء** (لم تُفترض): المكان `/admin/doctors` كصفحة مستقلة (وليس قسمًا داخل الإعدادات)، الصلاحية هي `manageStaff` نفسها (لا صلاحية `manageDoctors` جديدة)، سجل `Doctor` **يبقى منفصلًا تمامًا عن `Staff`** (لا حساب دخول للطبيب، لا علاقة جديدة في المخطط)، والحقول الحالية (اسم أول/أخير، اختصاص، هاتف، بريد) كافية دون إضافة حقول جديدة.
- **الميزات**: إضافة طبيب، تعديل بياناته، تفعيل/تعطيل (`isActive`) — **لا حذف نهائي**، لتفادي كسر قيد المفتاح الأجنبي على `Appointment`/`DentalVisit`/`TreatmentPlan`/`Invoice` (كلها إلزامية `doctorId`)؛ التعطيل هو المكافئ العملي للحذف دون فقدان السجلات التاريخية المرتبطة.
- **قاعدة البيانات**: إضافة عمود واحد فقط (`Doctor.isActive Boolean @default(true)`) — إضافي بحت، كل الأطباء الحاليين أصبحوا `true` تلقائيًا، لا فقدان بيانات.
- **الطبيب المعطَّل يختفي من فلتر الطبيب واختيار الطبيب في تقويم المواعيد** (`prisma.doctor.findMany` في `admin/appointments/page.tsx` أصبح يفلتر `isActive: true`) لكن **مواعيده/زياراته/فواتيره التاريخية تبقى كما هي** ولا تزال تُعرض في التقويم بلونها الافتراضي (خريطة الألوان تتعامل مع طبيب غير موجود في القائمة بأمان عبر قيمة احتياطية `var(--primary)`، لم تكن بحاجة لأي تعديل).
- **لا تكامل مع مركز الإشعارات لهذه الوحدة** (قرار مقصود لهذه الجولة، لم يُطلب) — إضافة/تعديل/تعطيل طبيب لا يُولِّد إشعارًا حاليًا، فقط سجل في `ActivityLog` (`CREATE_DOCTOR`/`UPDATE_DOCTOR`/`ENABLE_DOCTOR`/`DISABLE_DOCTOR`). يمكن إضافته لاحقًا إن رغبت العيادة (يتطلب قيمة جديدة في enum `NotificationType` + migration).

**التقارير والتحليلات** (`/admin/reports`): استبدلت بالكامل الصفحة البسيطة السابقة (إيرادات يومية/شهرية + إجراءات شائعة + مرضى نشطون + أرصدة مستحقة) بلوحة تحليلات كاملة، بدون أي تعديل على المخطط (صفر migrations هذه الجولة — **إعادة استخدام كاملة للجداول الموجودة** كما طُلب صراحة).
- **10 بطاقات إحصائية**: إيرادات اليوم/الشهر/العام، إجمالي المرضى، مرضى جدد هذا الشهر، مواعيد اليوم، مواعيد قادمة، أرصدة مستحقة، علاجات مكتملة، خطط علاج نشطة. هذه البطاقات **مرتبطة بفترات ثابتة** (اليوم/الشهر الحالي/العام الحالي فعليًا كما تقول أسماؤها) وتُصفَّى فقط حسب **فلتر الطبيب** — فلتر الفترة (اليوم/الأسبوع/الشهر/العام/مخصص) لا يُغيّر معنى هذه البطاقات تحديدًا (قرار تصميم مقصود لتفادي غموض "ما معنى تغيير 'إيرادات الشهر' عبر فلتر 'هذا الأسبوع'؟")، بل يُطبَّق على قسم الرسوم البيانية "التفصيلية" أدناه.
- **8 رسوم بيانية**، مقسَّمة إلى نوعين حسب الغرض (موثَّق بعنوان فرعي صغير تحت كل رسم في الواجهة نفسها لتفادي الالتباس):
  - **اتجاهات ثابتة (آخر 12 شهرًا دائمًا، بغض النظر عن فلتر الفترة، تتأثر بفلتر الطبيب فقط)**: الإيرادات الشهرية، اتجاه المرضى الجدد، نمو المرضى التراكمي.
  - **تحليل ضمن الفترة المحدَّدة (يتأثر بفلتري الفترة والطبيب معًا)**: الإيرادات حسب الخدمة، توزيع حالات المواعيد (يعيد استخدام `StatusDonutChart` الموجود مسبقًا **بلا أي تعديل**)، طرق الدفع، أكثر الإجراءات شيوعًا، أداء الأطباء.
  - **أداء الأطباء مخفي كليًا عن موظف الاستقبال** (الطبيب فقط يراه) — هذا هو التفسير المتفَق عليه صراحة مع المستخدم لعبارة "تقارير محدودة لموظف الاستقبال"، نظرًا لأن موظف الاستقبال يملك أصلًا `viewBilling`/`manageInvoices`/`managePayments` في بقية التطبيق فلا معنى لإخفاء أرقام الإيرادات نفسها عنه، لكن مقارنة أداء الأطباء ببعضهم تحديدًا تُعتبر معلومة حسّاسة لا يحتاجها هذا الدور. **لم يتغيَّر أي شيء في `src/lib/auth/permissions.ts`** — التمييز يتم بفحص مباشر `staff.role === "DOCTOR"` داخل صفحة التقارير نفسها فقط، وليس عبر نظام الصلاحيات العام.
  - **لا رسم بياني بمحورين (dual-axis)** في أي مكان — "أداء الأطباء" مثال: بدل محورين (إيراد + عدد مواعيد) على نفس الرسم، استُخدم رسم بمحور واحد (الإيراد) بالألوان القياسية لكل طبيب (نفس `buildDoctorColorMap` المستخدمة أصلًا في تقويم المواعيد، لتطابق الهوية اللونية) + جدول نصي صغير أسفله يعرض المقياس الثاني (عدد المواعيد المكتملة) كنص عادي.
- **الفلاتر**: أزرار فترة (اليوم/هذا الأسبوع/هذا الشهر/هذا العام/نطاق مخصص) + حقلي تاريخ عند اختيار "مخصص" + قائمة اختيار طبيب (تظهر فقط إن وُجد أكثر من طبيب فعّال، بنفس نمط شريط أدوات التقويم) — كل ذلك عبر `searchParams` في الرابط (`range`, `from`, `to`, `doctorId`) بنفس نمط تقويم المواعيد تمامًا (يدعم الرجوع والمشاركة كرابط مباشر). قيم فترة غير صحيحة أو نطاق مخصص غير منطقي (`from` بعد `to`، تواريخ غير صالحة) تتراجع تلقائيًا وبأمان إلى فلتر "هذا الشهر" الافتراضي (لا خطأ 500، مُختبَر فعليًا).
- **التصدير**: 3 أزرار — **CSV** (بدون أي مكتبة، توليد نص مباشر مع BOM في البداية ليظهر النص العربي بشكل صحيح عند الفتح في Excel) و**Excel** (عبر مكتبة `xlsx`/SheetJS الجديدة — **راجع ملاحظة أمنية أدناه**، ملف حقيقي متعدد الأوراق، ورقة منفصلة لكل جدول بيانات) و**PDF** (زر "طباعة" يستدعي `window.print()` ويعيد استخدام تصميم `print-area`/`no-print` الموجود مسبقًا في `globals.css` والمُستخدَم أصلًا لطباعة الفواتير — **لا مكتبة PDF جديدة**، بنفس القرار الذي أكّده المستخدم صراحةً).
  - **⚠️ ملاحظة أمنية موثَّقة عمدًا**: مكتبة `xlsx` (SheetJS) المتوفرة على npm تحمل ثغرتين معروفتين غير مُصلَحتين حاليًا (Prototype Pollution، ReDoS) — كلتاهما فقط في مسار **قراءة/تحليل** ملفات خارجية غير موثوقة. الكود هنا **لا يستخدم إلا مسار الكتابة** (`XLSX.utils.aoa_to_sheet` + `XLSX.writeFile` على بيانات محسوبة من الخادم نفسه، لا قراءة لأي ملف مرفوع من المستخدم) — تم عرض هذا صراحةً على المستخدم وتأكيد المتابعة رغم تنبيه `npm audit`.
- **الأداء**: كل الاستعلامات مُجمَّعة في قاعدة البيانات (`aggregate`/`groupBy`/`count`) — لا جلب صفوف خام وجمعها في الكود. **اتجاهات الـ12 شهرًا** (الإيرادات الشهرية، اتجاه المرضى الجدد) تُحسَب عبر **استعلام SQL خام واحد لكل مقياس** (`$queryRaw` مع `date_trunc('month', ...)`، أول استخدام لـ`$queryRaw` في المشروع) بدل 12 استعلامًا منفصلًا لكل شهر كما كان في النمط القديم لصفحة التقارير — تحسين أداء مباشر ومقصود استجابةً لطلب "Optimize queries". "نمو المرضى التراكمي" **لا يُنفِّذ استعلامًا رابعًا منفصلًا** بل يُشتَق حسابيًا من نفس نتيجة "اتجاه المرضى الجدد" + عدد أساسي واحد (مرضى قبل بداية النافذة الزمنية) — تطبيق مباشر لطلب "Avoid duplicated queries". "أداء الأطباء" يستخدم استعلامي `groupBy` فقط (فواتير + مواعيد مكتملة، كل واحد مجمَّع حسب `doctorId`) بدل استعلام منفصل لكل طبيب على حدة (لا N+1).
- **قاعدة البيانات: صفر migrations** — **إعادة استخدام كاملة** لكل الجداول الموجودة (`Payment`, `Invoice`, `InvoiceItem`, `Appointment`, `Patient`, `TreatmentPlan`, `TreatmentItem`, `Doctor`) بدون أي عمود أو نموذج جديد، تمامًا كما طُلب صراحة ("Reuse existing tables. No duplicate models").
- **مكوّنات رسوم بيانية عامة جديدة** (`src/components/admin/reports/`): `GenericBarChart` (عمودي/أفقي، تنسيق رقم أو عملة عبر خاصية نصية `valueFormat` وليس دالة — الدوال لا يمكن تمريرها من مكوّن خادم إلى مكوّن عميل في Next.js، خطأ فعلي ظهر وأُصلح أثناء هذه الجلسة، راجع الملاحظات التنفيذية)، `GenericAreaChart`، `GenericDonutChart`، `DoctorPerformanceChart`. أُعيد استخدام `StatusDonutChart` الموجود مسبقًا بلا أي تعديل عليه. حُذف مكوّن `revenue-bar-chart.tsx` القديم (كان يُستخدم فقط في صفحة التقارير السابقة، أصبح كودًا ميتًا تمامًا بعد إعادة البناء، تم التحقق أنه غير مُستورَد من أي مكان آخر قبل حذفه).

**البحث الشامل الذكي** (`Ctrl+K`/`⌘K` من أي صفحة إدارة): لوحة أوامر منبثقة تبحث عبر 8 فئات دفعة واحدة، بصلاحيات مُطبَّقة فعليًا لكل فئة.
- **⚠️ اكتشاف مهم أول شيء في هذه الجلسة**: كان يوجد فعليًا مكوّن `src/components/admin/global-search.tsx` **جزئي وغير موثَّق** مُستورَد بالفعل في `admin-shell.tsx` من جولة سابقة لم تُسجَّل في `PROJECT_STATUS.md`/`NEXT_TASK.md` — بحث بسيط عن المرضى فقط (`searchPatientsGlobal` في `admin/actions.ts`)، **بلا أي فحص صلاحية إطلاقًا** (أي موظف مسجّل دخول كان يمكنه البحث عن كل المرضى بغض النظر عن `viewPatients`). هذه الجولة استبدلت هذا المكوّن بالكامل بالمواصفات المطلوبة، وأصلحت ثغرة الصلاحية هذه ضمنًا (كانت جزءًا مما تطلبه "Respect user roles" أصلًا، وليست تعديلًا خارج النطاق). حُذفت `searchPatientsGlobal`/`PatientSearchResult` من `admin/actions.ts` بعد التأكد أنهما غير مُستوردتين من أي مكان آخر.
- **8 فئات بحث، كل فئة بصلاحيتها الخاصة** (`src/app/admin/search-actions.ts`، دالة `globalSearch(query)`): المرضى (`viewPatients`)، المواعيد (`viewAppointments`)، خطط العلاج والزيارات السريرية (كلاهما `viewMedicalRecord`)، الفواتير والدفعات (كلاهما `viewBilling`)، الموظفون (`manageStaff`)، الإشعارات (تُعيد استخدام `getVisibleNotificationTypes(role)` الموجودة أصلًا من مركز الإشعارات، بدل منطق صلاحية جديد). **كل فئة غير مسموحة للدور الحالي لا تُنفِّذ استعلامها إطلاقًا** (شرط `can(category) ? query : Promise.resolve([])` داخل `Promise.all` واحد) — تطبيق مباشر لـ"Avoid unnecessary database requests".
- **الحد الأقصى 5 نتائج لكل فئة** (`take: 5`) وحد أدنى حرفين قبل تنفيذ أي استعلام (`MIN_QUERY_LENGTH`) — لا يصل أي طلب لقاعدة البيانات لسؤال أقل من حرفين.
- **واجهة**: لوحة أوامر (`Dialog` موجود مسبقًا في `src/components/ui/dialog.tsx`، بلا أي تعديل عليه — فقط `className` مُخصَّص لتوسيعها وتثبيتها أعلى الشاشة بدل التمركز الافتراضي)، مُفعَّلة بزر في الشريط العلوي أو اختصار `Ctrl+K`/`⌘K` من أي مكان (مستمع `keydown` على `document` داخل المكوّن نفسه، الذي يُركَّب فعليًا في `AdminShell` على كل صفحة إدارة). بحث مؤجَّل (`debounce` 250ms)، تمييز النص المطابق داخل العنوان/العنوان الفرعي (`<mark>`)، تنقّل بلوحة المفاتيح (↑↓ للتنقّل بين كل النتائج مُسطَّحة عبر كل الفئات، Enter للانتقال، Esc للإغلاق)، عمليات بحث أخيرة (حتى 8، محفوظة في `localStorage` فقط — **لا جدول قاعدة بيانات جديد**، قرار مقصود يتماشى مع كون هذه بيانات عرضية خاصة بالمتصفح لا تحتاج مزامنة بين الأجهزة) مع زر "مسح السجل".
- **⚠️ درس React مهم اكتُشف وأُصلح أثناء هذه الجلسة**: قاعدة lint جديدة (`react-hooks/set-state-in-effect`) رفضت أي `setState` متزامن داخل جسم `useEffect` مباشرة (أربع حالات مختلفة في هذا المكوّن). الإصلاح لم يكن بتجاهل القاعدة بل بإعادة الهيكلة الصحيحة: (1) قراءة `localStorage` عند التحميل تحوّلت من effect+setState إلى **مُهيِّئ كسول** `useState(() => readRecentSearches())` (آمن هنا لأن محتوى لوحة الأوامر لا يُعرَض إطلاقًا إلا بعد فتحها فعليًا، فلا تعارض Hydration)، (2) إعادة تعيين الحالة عند إغلاق اللوحة تحوّلت من effect مرتبط بـ`open` إلى دالة `closeSearch()` واحدة تُستدعى مباشرة من كل مسار إغلاق فعلي (Esc، زر X، اختيار نتيجة، النقر خارج اللوحة)، (3) `setLoading(true)` انتقل من بداية الـ effect إلى داخل رد نداء `setTimeout` (بعد انتهاء فترة الـ debounce فعليًا، وهو أصح دلاليًا أيضًا — لا داعٍ لإظهار مؤشر تحميل أثناء انتظار الـ debounce نفسه). **قاعدة عامة أخرى مفيدة اكتُشفت**: لا تُراكم عدادًا متغيرًا (`let index = -1؛ index++`) أثناء الترسيم (render) — استُبدل بخريطة `Map` مبنية مسبقًا عبر `useMemo`.
- **روابط التنقّل لكل فئة**: مريض ← `/admin/patients/{id}`، موعد ← `/admin/appointments?view=day&date={yyyy-mm-dd}` (لا صفحة تفصيل مستقلة للموعد أصلًا، فالتنقّل لعرض يوم التقويم هو الأقرب الممكن دون تعديل التقويم نفسه)، خطة علاج/زيارة سريرية ← `/admin/patients/{id}#treatment-plans` أو `#dental-visits` (أُضيف `id` إلى غلاف كل من `TreatmentPlansSection`/`DentalVisitsSection` خصيصًا لهذا — إضافة HTML بحتة غير مرئية، لا تغيير تصميمي)، فاتورة/دفعة ← `/admin/billing/{invoiceId}`، موظف ← `/admin/staff/{id}`، إشعار ← حقل `link` الموجود أصلًا على الإشعار نفسه أو `/admin/notifications` كبديل.

**صلاحيات الأدوار الأربعة الجديدة + حماية صفحات المرضى/المواعيد** (`src/lib/auth/permissions.ts`, `src/app/admin/patients/page.tsx`, `src/app/admin/appointments/page.tsx`): أغلقت الفجوة الموصى بها عبر ثلاث جولات متتالية سابقة. **الإجابات جاءت صراحة من المستخدم قبل الكتابة** (لم تُفترض):
- **`ASSISTANT`/`HYGIENIST`** (مساعد طبي/أخصائي نظافة): "اطّلاع سريري فقط" — `viewAppointments`, `viewPatients`, `viewMedicalRecord`. بلا تعديل، بلا فوترة، بلا صلاحيات إدارية.
- **`ACCOUNTANT`** (محاسب): "فوترة وتقارير فقط" — `viewBilling`, `manageInvoices`, `managePayments`, `viewReports`, `viewStatistics`. بلا وصول لملفات المرضى الطبية أو المواعيد.
- **`ADMIN`** (مدير النظام): "إبقاء كما هو" — يبقى `manageStaff` فقط، لم يُوسَّع.
- **`DOCTOR`/`RECEPTIONIST`**: لم يُمسّا إطلاقًا.
- **حماية الصفحات**: أُضيف فحص `hasPermission(staff.role, "viewPatients"/"viewAppointments")` + `redirect("/admin")` في بداية كل من `PatientsPage`/`AppointmentsPage` — بنفس النمط الحرفي المستخدم مسبقًا في `/admin/staff`/`/admin/billing` (تحويل كامل 307، وليس عرضًا جزئيًا — هذا ما اختاره المستخدم صراحة).
- **لا تعديل على `search-actions.ts` أو `notifications.ts` أو `reports`/`page.tsx`** — الثلاثة تعتمد على `hasPermission()`/`getVisibleNotificationTypes()` المركزيتين مباشرة، فانعكس التوسيع تلقائيًا (مُختبَر ومؤكَّد أدناه) بصفر أسطر كود إضافية في تلك الوحدات.
- **صفر migrations** — تعديل منطقي بحت في `permissions.ts` (تغيير محتوى مصفوفات ثابتة)، لا مساس بالمخطط.

**⚠️ ملاحظة لهذه الجولة**: إعادة هيكلة `src/app/` (الوحدة 14 أدناه) نقلت `admin`/`login` فعليًا إلى `src/app/(admin)/admin`/`src/app/(admin)/login` (route group) — **المسارات الفعلية في المتصفح (`/admin/*`, `/login`) لم تتغيّر إطلاقًا**، فقط موقع الملفات على القرص. أي مسار كود مذكور أعلاه بصيغة `src/app/admin/...` أصبح فعليًا `src/app/(admin)/admin/...`.

**الموقع العام ثنائي اللغة (عربي/إنجليزي)** (`src/app/(public)/[lang]/`, `src/lib/i18n/`): جعلت **الموقع العام وصفحة الحجز فقط** ثنائيي اللغة — **لوحة الإدارة تبقى عربية بالكامل ولم تُمس إطلاقًا** (تحقُّق فعلي بـ18 فحص HTTP بجلسات حقيقية، راجع الاختبار أدناه).
- **إعادة هيكلة المسارات وفق الدليل الرسمي لـ Next.js نفسه** (`node_modules/next/dist/docs/01-app/02-guides/internationalization.md` + نمط "multiple root layouts" عبر route groups من `.../file-conventions/route-groups.md`، قُرئا فعليًا قبل الكتابة كما تقتضي تعليمات المشروع): `src/app/(public)/[lang]/` (الصفحة الرئيسية + الحجز، بـ`layout.tsx` جذري خاص بها بـ`<html lang dir>` ديناميكي) مقابل `src/app/(admin)/` (كل admin/login، نسخة طبق الأصل حرفيًا من `layout.tsx` القديم، بلا أي تعديل سلوكي). الاستثناء الوحيد: أُزيل `BookingDialogProvider` من نسخة admin فقط (كان غلافًا ميتًا غير مستخدَم أصلًا هناك — لا مكوّن admin يستدعي `useBookingDialog()`، تم التحقق) لأن نافذة الحجز أصبحت تعتمد على سياق لغة غير متاح في admin.
- **الاستراتيجية: العربية بلا بادئة (لا كسر لأي رابط حالي)، الإنجليزية ببادئة `/en`** — `/` و`/appointment` يعملان تمامًا كما كانا، `/en` و`/en/appointment` للنسخة الإنجليزية. يُنفَّذ عبر `src/proxy.ts`: أي مسار عام غير مسبوق بـ`/en` يُعاد كتابته داخليًا (invisible rewrite) إلى `/ar/...`، إلا إن كان تفضيل الزائر المحفوظ (كوكي `NEXT_LOCALE`) إنجليزيًا فيُحوَّل (redirect) إلى `/en/...` ليطابق الرابط الظاهر التفضيل. منطق `updateSession` الحالي لـ`/admin`/`/login` في نفس الملف **لم يتغيّر حرفيًا**، فقط أُضيف فرع جديد بجانبه.
- **القاموس**: كائنات TypeScript عادية (لا JSON، لا مكتبة خارجية جديدة) في `src/lib/i18n/dictionaries/{ar,en}.ts` — نوع القاموس الإنجليزي مُشتَق من العربي عبر `satisfies Dictionary`، فأي مفتاح ترجمة ناقص في أي من اللغتين يفشل عليه `tsc`/`build` فورًا (مُختبَر فعليًا أثناء هذه الجلسة: نسيان `as const` سبّب فشل البناء بسبب تضييق الأنواع الحرفي، أُصلح بإزالتها). `get-dictionary.ts` بنمط `server-only` مطابق لنمط `reports.ts`/`create-notification.ts` الموجود.
- **توصيل القاموس للمكوّنات**: مكوّن خادم جديد `PublicI18nProvider`/`usePublicI18n()` (نفس نمط `ClinicSettingsProvider`/`useClinicSettings()` الموجود مسبقًا حرفيًا) — ضروري (وليس اختياريًا) لأن نافذة الحجز تُركَّب من مزوّد أعلى من أي صفحة، فلا يكفي تمرير prop من الصفحة. مكوّن `Footer` الوحيد الذي بقي Server Component يستقبل القاموس عبر prop مباشر (لا يمكنه استخدام Context).
- **بيانات العيادة الديناميكية (اسم العيادة/الطبيب/العنوان/الهاتف/ساعات الدوام) تبقى بالعربية دائمًا حتى في النسخة الإنجليزية** — **قرار مؤكَّد صراحة من المستخدم قبل الكتابة** (لا حقل إنجليزي في قاعدة البيانات، وصفحة الإعدادات تبقى عربية بلا أي تعديل كما طُلب). فقط النصوص الثابتة (عناوين، أزرار، تسميات، الأسئلة الشائعة، آراء المرضى...) هي المُترجمة.
- **"الأطباء" في قائمة الترجمة المطلوبة** — **قرار مؤكَّد صراحة من المستخدم**: تُرجم قسم "عن الدكتور" الموجود فعليًا (عيادة طبيب واحد، لا قائمة أطباء عامة)، لم يُبنَ قسم جديد.
- **نموذج الحجز وvalidation**: `src/lib/validations/appointment.ts` (خاص بالحجز العام حصرًا، تم التحقق أنه غير مُستخدَم في admin) تحوَّل من مخطط Zod ثابت إلى دالة مصنع `getAppointmentFormSchema(dict)` تُستدعى بقاموس اللغة الحالية، من العميل (للتحقق الفوري) والخادم (`createAppointment` في `actions.ts`، استقبل معاملًا جديدًا `locale`) معًا — رسائل الخطأ تُطابق لغة الزائر في الحالتين.
- **⚠️ قيد مهم مقصود داخل `createAppointment`**: استدعاءات `createNotification(...)` التي تُغذّي مركز إشعارات **admin** ("مريض جديد"، "موعد جديد"...) **تبقى عربية ثابتة دائمًا بغض النظر عن لغة الزائر** — لم تُربط بمعامل `locale` الجديد عمدًا (لوحة الإدارة عربية فقط). **مُختبَر فعليًا**: حجز تجريبي كامل باللغة الإنجليزية أنتج رسائل نجاح/تحقق إنجليزية للزائر، بينما ظل إشعارا "موعد جديد"/"مريض جديد" في قاعدة البيانات عربيَّين تمامًا كما هما.
- **لا نظام بريد إلكتروني موجود أصلًا في المشروع** (تم التحقق: لا `nodemailer`/`resend`/أي مكتبة بريد). "رسائل التأكيد بلغة الزائر" تعني فعليًا رسالة التوست/التأكيد داخل الصفحة فقط — إرسال بريد فعلي فجوة موجودة مسبقًا وخارج نطاق هذه الجولة (لم يُطلَب بناء نظام بريد جديد).
- **RTL/LTR تلقائي بالكامل**: Tailwind v4 يدعم الخصائص المنطقية أصلًا (بلا أي تعديل إعداد). استُبدلت مجموعة صغيرة من الأصناف ذات الاتجاه الفعلي (`text-right`→`text-start`، `right-*`/`left-*`→`end-*`/`start-*`) في `hero.tsx`، `footer.tsx`، `about-doctor.tsx`، `treatment-steps.tsx`، `contact.tsx` بمكافئاتها المنطقية — نفس المظهر تمامًا تحت `dir="rtl"` الحالي، مع انعكاس صحيح تلقائي تحت `dir="ltr"` للإنجليزية. **لا تعديل تصميمي فعلي**.
- **ساعات الدوام** (`src/lib/working-hours.ts`، مُستخدَمة أيضًا في محرِّر ساعات الدوام بلوحة الإدارة `working-hours-editor.tsx`): أُضيف معامل اختياري ثالث `locale: "ar" | "en" = "ar"` — القيمة الافتراضية تُبقي سلوك admin الحالي مطابقًا حرفيًا (لم يُستدعَ بمعامل جديد هناك)، وقسم "تواصل معنا" العام فقط يمرّر `"en"` عند الحاجة.
- **مبدّل اللغة** (`src/components/layout/language-switcher.tsx`, في الشريط العلوي وقائمة الموبايل): 🇸🇦 العربية / 🇬🇧 English، يحفظ الاختيار في كوكي `NEXT_LOCALE` (سنة كاملة) عبر `document.cookie` مباشرة من دالة مساعدة خارج نطاق المكوّن (تفاديًا لخطأ lint حقيقي ظهر فعليًا: `react-hooks/immutability` يرفض تعديل `document` مباشرة داخل جسم مكوّن/hook — أُصلح بنقل السطر إلى دالة عادية خارج المكوّن، وليس بتجاهل القاعدة، بنفس نمط الإصلاحات الموثَّقة في جلسات سابقة).
- **SEO لكل لغة**: `generateMetadata` في `layout.tsx`/`appointment/page.tsx` لكل لغة (عنوان، وصف، Open Graph، `alternates.languages` بروابط hreflang لـ`ar`/`en`/`x-default`) — **مُختبَر فعليًا عبر HTML الفعلي المُستلَم بـ`curl`**.
- **صفر migrations، صفر مكتبات جديدة** — القاموس والتوجيه كلاهما مبنيان يدويًا بالكامل وفق نمط Next.js الموثَّق رسميًا، بلا `next-intl` أو أي حزمة i18n خارجية.

---

## 🧪 الوحدات المُختبرة (Tested Modules)

| الوحدة | طريقة الاختبار | النتيجة |
|---|---|---|
| الموقع العام + الحجز | متصفح Chrome حقيقي (مراحل سابقة) | ✅ صفر أخطاء Console |
| لوحة الإدارة + RBAC | متصفح Chrome حقيقي (مراحل سابقة) | ✅ |
| EDR | متصفح Chrome حقيقي (مراحل سابقة) | ✅ |
| العلاج والفوترة | متصفح Chrome حقيقي — إنشاء خطة/فاتورة/دفعة، صلاحيات موظف الاستقبال | ✅ صفر أخطاء Console |
| إعدادات العيادة | متصفح Chrome حقيقي — تعديل/رفع شعار/ألوان/ساعات دوام/صلاحيات | ✅ صفر أخطاء Console، `next build` ناجح |
| إدارة الموظفين | HTTP + جلسات + قاعدة بيانات مباشرة (لا متصفح متاح) | ✅ راجع التفاصيل أدناه |
| تقويم المواعيد المتقدم | منطق مباشر (قاعدة بيانات) + HTTP للصفحات + استخدام حقيقي فعلي من المستخدم لاحظته سجلات الخادم | ✅ راجع التفاصيل أدناه |
| مركز الإشعارات | منطق مباشر + Realtime حقيقي مُختبَر فعليًا (اشتراك + إدراج + استقبال) + HTTP بأدوار متعددة | ✅ راجع التفاصيل أدناه |
| إدارة الأطباء | `tsc`/`eslint`/`build` + منطق مباشر (7 اختبارات) + HTTP بحساب طبيب حقيقي وحساب موظف استقبال حقيقي | ✅ راجع التفاصيل أدناه |
| التقارير والتحليلات | `tsc`/`eslint`/`build` + مطابقة حسابية مباشرة (17 اختبارًا) + منطق تصدير حقيقي (9 اختبارات، تضمّن قراءة ملف xlsx حقيقي) + HTTP بحسابين حقيقيين (طبيب/موظف استقبال) بكل قيم الفلاتر | ✅ راجع التفاصيل أدناه |
| البحث الشامل الذكي | `tsc`/`eslint`/`build` + منطق صلاحيات مباشر (9 اختبارات، 4 أدوار مختلفة) + قياس سرعة استعلام فعلي + HTTP بثلاثة حسابات حقيقية (طبيب/موظف استقبال/مدير نظام) | ✅ راجع التفاصيل أدناه |
| صلاحيات الأدوار الأربعة + حماية صفحات المرضى/المواعيد | `tsc`/`eslint`/`build` + منطق مباشر (33 اختبارًا: `hasPermission` لكل دور/صلاحية + `getVisibleNotificationTypes`) + HTTP بأربعة حسابات حقيقية (Assistant/Hygienist/Accountant/Admin) على 4 مسارات لكل حساب (16 فحصًا) | ✅ راجع التفاصيل أدناه |
| **الموقع العام ثنائي اللغة** | **`tsc`/`eslint`/`build` (بعد حذف `.next` القديم) + HTTP لتوجيه اللغة (`/`, `/appointment`, `/en`, `/en/appointment`, تفضيل الكوكي) + انحدار admin كامل (18 فحصًا بجلستي طبيب/موظف استقبال حقيقيتين) + حجز فعلي كامل باللغة الإنجليزية + فحص hreflang/العنوان/الوصف من HTML الفعلي** | ✅ راجع التفاصيل أدناه |

### كيف اختُبرت إدارة الموظفين تحديدًا
- `npm run build` و`tsc --noEmit`: نجاح تام بدون أخطاء.
- تم إنشاء حساب Admin تجريبي حقيقي في Supabase Auth + Prisma، وتسجيل الدخول به فعليًا (عبر استدعاء برمجي مباشر لـ Supabase، وليس محاكاة)، ثم:
  - `/admin/staff`, `/admin/staff/new`, `/admin/staff/[id]` → **200** لهذا الحساب (يملك `manageStaff`).
  - `/admin/billing`, `/admin/settings/clinic` → **307** لنفس الحساب (لا يملك تلك الصلاحيات — سلوك صحيح متوقَّع).
  - صفحة الملف الشخصي عرضت كل الحقول الجديدة بشكل صحيح (مسمى وظيفي، ملاحظات، تاريخ تعيين، بريد).
- تم اختبار حساب موظف الاستقبال التجريبي الموجود مسبقًا: `/admin/staff/*` → **307** (محظور بشكل صحيح)، بقية الصفحات لم تتأثر.
- تم اختبار مسار الكتابة الكامل مباشرة (إعادة تعيين كلمة مرور + تسجيل دخول فعلي بها، تعديل بريد إلكتروني مع مزامنة Supabase Auth، رفض قيد فرادة البريد المكرر، حذف كامل للحساب) — كل ذلك نجح، ثم حُذف الحساب التجريبي بالكامل (لا أثر متبقٍ).
- **لم يُختبر بصريًا في متصفح حقيقي**: القوائم المنسدلة لاختيار الدور، رفع صورة من واجهة المستخدم، سلوك الفورم التفاعلي. الكود يتبع نفس الأنماط المُختبرة فعليًا في وحدات سابقة (Select/AssetUploadField) لكن لم يُشاهَد بصريًا هذه الجولة.

### كيف اختُبر تقويم المواعيد تحديدًا
- `npm run build`، `tsc --noEmit`، و`eslint` على كل الملفات الجديدة/المعدَّلة: نجاح تام بدون أخطاء (تم اكتشاف وإصلاح خطأ lint واحد فعليًا: استدعاء `setState` داخل `useEffect` في `NewAppointmentDialog` — أُصلح بالاعتماد على إعادة التركيب الطبيعية للـ Dialog عند الفتح بدل الـ effect، بنفس نمط بقية الـ Dialogs في المشروع).
- **سكربت تحقق مباشر بلا متصفح** شغّل 18 حالة اختبار حقيقية ضد منطق التقويم الفعلي (وليس محاكاة): دوال ساعات الدوام (`isWithinWorkingHours`, `isRangeWithinWorkingHours`, `getDisplayHourRange`) لأوقات ضمن/خارج الدوام ويوم مغلق، دوال حساب التواريخ (بداية الأسبوع من السبت، 7 أيام للأسبوع، 42 يومًا لشبكة الشهر)، واستعلام كشف تعارض السحب/الإفلات الفعلي مباشرة على قاعدة البيانات (إنشاء موعدين حقيقيين، محاولة نقل أحدهما إلى وقت متعارض مع الآخر فعليًا). **18/18 نجحت** — بما فيها اكتشاف وإصلاح خطأ حقيقي في تصميم الاختبار نفسه أثناء التنفيذ (وليس في الكود المُختبَر).
- تم إنشاء حساب طبيب تجريبي (صلاحيات كاملة) وتسجيل الدخول به فعليًا، ثم التحقق عبر HTTP:
  - `/admin/appointments` (تقويم، الوضع الافتراضي)، `?mode=list`، `?view=day`، `?view=month` ← **200** جميعها، بلا أي digest خطأ.
  - محتوى صفحة التقويم يحتوي فعليًا على كل عناصر الواجهة المتوقعة (أزرار اليوم/أسبوع/شهر/حجز جديد/عرض القائمة، أقسام الشريط الجانبي الثلاثة).
  - كل الصفحات السابقة (`/`, `/admin`, `/admin/patients`, `/admin/billing`, `/admin/settings/clinic`, `/admin/staff`) ما زالت **200** لنفس الحساب — لا كسر.
  - تم حذف الحساب التجريبي بالكامل بعد الاختبار.
- تم أيضًا التحقق أن حساب موظف الاستقبال التجريبي الموجود مسبقًا لا يزال يفتح كل أوضاع التقويم بلا مشاكل (لا كسر في صلاحياته الحالية).
- **ملاحظة غير معتادة أثناء هذه الجلسة**: ظهرت في سجلات الخادم أثناء الاختبار طلبات POST حقيقية (`rescheduleAppointment`, `updateAppointmentStatus` بحالة `NO_SHOW` الجديدة، `deleteAppointment`) لم تصدر من أي سكربت اختبار مكتوب في هذه الجلسة — يبدو أن المستخدم اختبر السحب/الإفلات وتغيير الحالة والحذف فعليًا عبر متصفح حقيقي أثناء التطوير، ونجحت كلها بلا أخطاء في السجلات.
- **لم يُختبر بصريًا من قِبل المساعد نفسه في متصفح حقيقي**: طبقة الرسم البصري نفسها (مواضع البكسل الدقيقة، انسيابية السحب أثناء الحركة، شكل خانات "+N أخرى" في عرض الشهر). المنطق تحته (الرياضيات الزمنية، فحص التعارض، فحص ساعات الدوام) مُختبَر فعليًا كما ورد أعلاه.

### كيف اختُبر مركز الإشعارات تحديدًا
- `npm run build`، `tsc --noEmit`، و`eslint`: نجاح تام (خطأ lint واحد حقيقي اكتُشف وأُصلح: تحديث ref مباشرة أثناء الترسيم في hook التحديث، أُصلح بنقله داخل `useEffect`).
- **سكربت منطق مباشر (15 اختبارًا)**: تحقق دقيق من `getVisibleNotificationTypes` لكل الأدوار الستة (الطبيب = كل الأنواع، موظف الاستقبال = المواعيد والمرضى فقط بالضبط، مدير النظام = `STAFF_CREATED` فقط لأنها صلاحيته الوحيدة، الأدوار الثلاثة الأخرى = صفر) + إنشاء إشعار حقيقي من كل الأنواع العشرة والتحقق من صحة الحقول + استعلامات الجلب المُصفَّاة حسب الدور + **تحديد الكل كمقروء المحدود بالنطاق** (تم التحقق فعليًا أن تنفيذه بصفة موظف استقبال يُحدِّث إشعار المواعيد لكن **لا يمس** إشعار "موظف جديد" غير المرئي له) + الحذف. **15/15 نجحت**.
- **اختبار Realtime حقيقي من طرف إلى طرف** (وليس افتراضًا بأنه "متاح"): تم أولًا التحقق تجريبيًا من قاعدة البيانات أن نشر `supabase_realtime` موجود فعلًا على هذا المشروع (استعلام `pg_publication` مباشر) قبل كتابة أي migration يعتمد عليه. ثم تم إنشاء حساب اختبار حقيقي، تسجيل الدخول به، فتح اشتراك Realtime فعلي (نفس طريقة عميل المتصفح تمامًا)، إدراج صف إشعار حقيقي من اتصال Prisma منفصل، والتحقق من استقبال الحدث خلال أقل من ثانية. **نجح فعليًا** (تمت ملاحظة أن أول محاولة بلا فترة سماح بعد `SUBSCRIBED` فشلت بسبب تأخر انتشار الاشتراك على الخادم — سلوك طبيعي موثَّق لـ Supabase Realtime، لا يؤثر على الكود الفعلي لأن مكوّنات التطبيق تبقى مشتركة باستمرار وليست اشتراكًا لحظيًا واحدًا).
- **HTTP بأدوار متعددة حقيقية**: حساب طبيب تجريبي (رأى 3 إشعارات اختبار من أنواع مختلفة بعلامات أولوية صحيحة "حرجة/عالية/متوسطة" ظاهرة في HTML الفعلي) وحساب موظف الاستقبال التجريبي الموجود مسبقًا (رأى إشعار المواعيد فقط، **لم** يرَ إشعاري الفاتورة والموظف — تم التحقق من العدد الفعلي لكل نص في HTML المُستلَم، وليس افتراضًا). كل المسارات السابقة بقيت 200/307 كما هي لكلا الحسابين. تم حذف الحساب التجريبي وكل بيانات الاختبار بالكامل بعد الانتهاء.
- **لم يُختبر بصريًا في متصفح حقيقي**: انسيابية القائمة المنسدلة للجرس، ظهور التحديث اللحظي فعليًا أمام المستخدم أثناء التصفح (تم التحقق من آلية النقل التقنية نفسها فقط عبر السكربت أعلاه، وليس التجربة البصرية الكاملة في المتصفح).

### كيف اختُبرت إدارة الأطباء تحديدًا
- `npx tsc --noEmit`، `npx eslint` على كل الملفات الجديدة، و`npm run build`: نجاح تام بدون أخطاء (بعد `prisma generate` + إعادة تشغيل `next dev` بعد الـ migration، كالمعتاد).
- **سكربت منطق مباشر (7 اختبارات) مباشرة على قاعدة البيانات الحقيقية**: إنشاء طبيب بـ`isActive` افتراضي `true`، التحقق أن فحص تكرار الهاتف (الذي يُنفَّذ في `createDoctor` قبل الإنشاء) يجد الطبيب الموجود فعليًا، إنشاء طبيبين بلا هاتف/بريد للتأكد أن القيد الفريد (`@unique`) لا يتعارض مع قيم `NULL` متعددة، تعطيل طبيب والتحقق أنه يختفي من استعلام "الأطباء الفعّالون" (نفس الاستعلام المستخدم في تقويم المواعيد) بينما يبقى طبيب آخر فعّال ظاهرًا، إعادة التفعيل والتحقق. **7/7 نجحت**، ثم حُذفت كل بيانات الاختبار.
- **HTTP بحسابين حقيقيين عبر جلسة Supabase فعلية** (تسجيل دخول برمجي مباشر عبر `@supabase/ssr` بنفس آلية التطبيق الفعلية لالتقاط كوكي الجلسة الحقيقية، وليس محاكاة): حساب طبيب تجريبي (`manageStaff` ✓) → `/admin/doctors`, `/admin/doctors/new`, `/admin/doctors/[id]` (الطبيب الافتراضي الموجود) ← **200** جميعها، محتوى الصفحة يحتوي فعليًا على "إدارة الأطباء" واسم الطبيب الموجود وحالته "فعّال". حساب موظف الاستقبال التجريبي الموجود مسبقًا (بلا `manageStaff`) → `/admin/doctors`, `/admin/doctors/new` ← **307** (نفس سلوك `/admin/staff` تمامًا، كما هو متوقَّع). كل المسارات السابقة (بما فيها `/admin/appointments`, `/admin/patients`) بقيت كما كانت لكلا الحسابين — لا كسر.
- تم أيضًا التحقق أن كل صفحات لوحة الإدارة (13 مسارًا) لا تزال تُعيد **307** لزائر غير مسجّل دخول، وأن `/` و`/login` لا يزالان **200**.
- تم حذف الحساب التجريبي بالكامل (Supabase Auth + صف `Staff`) بعد الانتهاء؛ لم يتبقَّ أي طبيب تجريبي في جدول `Doctor` (تم التحقق مباشرة من محتوى الجدول: طبيب واحد فقط، "خليل الجمعة").
- **لم يُختبر بصريًا في متصفح حقيقي**: شكل الفورم وسلوك المفتاح (Switch) التفاعلي بصريًا، رغم أنه يتبع نفس مكوّن `Switch` المُختبَر بصريًا فعليًا في صفحة تعديل الموظف.

### كيف اختُبرت التقارير والتحليلات تحديدًا
- `npx tsc --noEmit`، `npx eslint src`، و`npm run build`: نجاح تام بدون أخطاء (خطأ واحد فعلي اكتُشف وأُصلح أثناء هذه الجلسة نفسها — راجع الملاحظات التنفيذية أدناه: تمرير دالة `valueFormatter` من مكوّن خادم (`page.tsx`) إلى مكوّن عميل (`GenericBarChart`) يفشل فورًا بخطأ 500 حقيقي في وقت التشغيل، رغم نجاح `tsc`/`build` الساكن — أُصلح باستبدال الدالة بخاصية نصية `valueFormat: "currency" | "number"` يُنفَّذها المكوّن العميل داخليًا).
- **سكربت مطابقة حسابية مباشرة (17 اختبارًا) ضد قاعدة البيانات الحقيقية**: كل بطاقة من البطاقات العشر قُورنت رقميًا باستعلام Prisma مستقل مكتوب يدويًا (وليس بإعادة استدعاء نفس الدالة) للتأكد أن منطق التجميع صحيح فعليًا وليس مجرد "يعمل بلا خطأ". تحقق إضافي: مجموع نقاط "الإيرادات الشهرية" (12 نقطة) يساوي استعلام تجميع مباشر لنفس النافذة الزمنية الكاملة (يتحقق من صحة استعلام `$queryRaw` الجديد بمقارنته باستعلام Prisma عادي منفصل تمامًا)، آخر نقطة في "نمو المرضى التراكمي" تساوي العدد الحالي الفعلي لكل المرضى في قاعدة البيانات، مجموع "توزيع حالات المواعيد" و"طرق الدفع" يساوي إجمالي المواعيد/الدفعات ضمن النطاق، فلترة حسب طبيب مُحدَّد تُعيد `doctorPerformance: null` وتُطابق استعلامًا مباشرًا مُصفَّى بنفس الطبيب. **17/17 نجحت**.
- **سكربت تحقق من منطق التصدير (9 اختبارات)**: التحقق أن `buildExportTables` تُنتج 9 جداول بالضبط (8 رسوم + أداء الأطباء)، أن كل جدول له عنوان، أن جدول الملخص يحوي كل البطاقات العشر. لملف CSV: التحقق الفعلي من وجود BOM UTF-8 في أول بايت (`charCodeAt(0) === 0xfeff`)، وأن النص العربي يبقى سليمًا. **لملف Excel: تم فعليًا بناء ملف `.xlsx` حقيقي بمكتبة `xlsx` وكتابته على القرص، ثم إعادة قراءته وفتحه للتحقق أن عدد الأوراق يطابق عدد الجداول وأن صف العناوين والبيانات العربية عادت سليمة تمامًا بعد الحفظ والقراءة الفعليين** (وليس افتراضًا أن المكتبة "يجب أن تعمل"). **9/9 نجحت**، ثم حُذف الملف المؤقت.
- **HTTP بحسابين حقيقيين عبر جلسة Supabase فعلية** (نفس الأسلوب المُختبَر في الجولات السابقة: `@supabase/ssr` مباشرة لالتقاط كوكي جلسة حقيقي دون محاكاة نموذج تسجيل الدخول): حساب طبيب تجريبي وحساب موظف الاستقبال التجريبي الموجود مسبقًا، كلاهما ← **200** على `/admin/reports` بكل قيم الفلتر (`range=today/week/month/year/custom`، فلتر طبيب محدَّد وغير موجود). تم التحقق **بعدّ النص الفعلي في HTML المُستلَم** أن "أداء الأطباء" يظهر لحساب الطبيب فقط وغائب تمامًا عن حساب موظف الاستقبال (تطابق حرفي مع القرار المُتَّفق عليه). تم أيضًا التحقق أن نطاقًا مخصصًا غير صالح (تاريخ غير صحيح، `from` بعد `to`، قيمة `range` غير معروفة) يتراجع بأمان لفلتر "الشهر" الافتراضي بدلًا من خطأ 500. كل المسارات السابقة (13 مسارًا) بقيت كما كانت لحساب الطبيب — لا كسر.
- تم حذف الحساب التجريبي بالكامل بعد الانتهاء (Supabase Auth + صف `Staff`)؛ لا بيانات اختبار متبقية في أي جدول.
- **لم يُختبر بصريًا في متصفح حقيقي**: النقر الفعلي على أزرار "CSV"/"Excel"/"PDF" وتحميل الملف عبر واجهة المتصفح (لا يوجد Playwright/Puppeteer فعّال في بيئة التطوير) — بدلًا من ذلك، اختير اختبار **منطق التوليد الفعلي بنفس الشيفرة والمكتبة** (بما فيها بناء وقراءة ملف xlsx حقيقي فعليًا، وليس افتراضًا) كأقرب تحقق ممكن دون متصفح. شكل الرسوم البيانية بصريًا (تراكب التسميات، التمرير على الموبايل) لم يُشاهَد أيضًا.

### كيف اختُبر البحث الشامل الذكي تحديدًا
- `npx tsc --noEmit`، `npx eslint src`، و`npm run build`: نجاح تام بدون أخطاء بعد إصلاح **4 أخطاء lint حقيقية فعلية** من قاعدة `react-hooks/set-state-in-effect` (`setState` متزامن داخل `useEffect`) + خطأ خامس (`react-hooks/immutability`، متغيّر مُعاد تعيينه أثناء الترسيم) — كلها أُصلحت بإعادة هيكلة صحيحة (مُهيِّئ كسول، دالة إغلاق موحَّدة، نقل `setLoading` داخل رد نداء `setTimeout`، خريطة `Map` مبنية مسبقًا بدل عدّاد متغيّر)، وليس بتجاهل القاعدة. راجع تفاصيل كل إصلاح في قسم الوحدة أعلاه والملاحظات التنفيذية أدناه.
- **⚠️ اكتُشف أولًا (قبل كتابة أي كود جديد)**: مكوّن بحث جزئي غير موثَّق موجود مسبقًا (`global-search.tsx` القديم يبحث في المرضى فقط، **بلا فحص صلاحية إطلاقًا**) — تم فحصه بالكامل والتأكد من عدم وجود أي وثيقة تصفه في `PROJECT_STATUS.md`/`NEXT_TASK.md` قبل استبداله، تفاديًا لتكرار عمل أو كسر شيء غير موثَّق كان المستخدم يعتمد عليه فعليًا.
- **سكربت منطق صلاحيات مباشر (9 اختبارات) على قاعدة البيانات الحقيقية**، بتمرير كل من الأدوار الأربعة (`DOCTOR`, `RECEPTIONIST`, `ADMIN`, `ASSISTANT`) مباشرة لمحاكاة `globalSearch` (بدل `getCurrentStaff()` التي تحتاج سياق طلب حقيقي لا يتوفر في سكربت مستقل): تحقُّق أن **`RECEPTIONIST` لا يرى الموظفين إطلاقًا** (لا `manageStaff`) بينما يرى المرضى/الفواتير/الدفعات بنفس عدد نتائج `DOCTOR` تمامًا (له تلك الصلاحيات فعليًا)، أن **`ADMIN` لا يرى المرضى/المواعيد/الفواتير/الزيارات/خطط العلاج إطلاقًا** (لا يملك أيًّا من صلاحياتها) بينما **يرى الموظفين بنفس عدد `DOCTOR`** (يملك `manageStaff`)، وأن **`ASSISTANT` لا يرى أي نتيجة في أي فئة إطلاقًا** (صفر صلاحيات حاليًا، يطابق قيود الأدوار الأربعة الموثَّقة). **9/9 نجحت** — هذا يثبت فعليًا (وليس افتراضًا) أن "Never show results that the logged-in user cannot access" مُطبَّق بدقة حرفية لكل فئة على حدة.
- **قياس سرعة استعلام فعلي** (5 تشغيلات بعد تشغيل تسخين واحد مُستبعَد من القياس، بحساب طبيب كامل الصلاحيات، على بيانات المرضى الحقيقية الموجودة فعلًا في قاعدة البيانات): متوسط **~47 مللي ثانية** لكل بحث متعدد الفئات (4 فئات فعلية شُغِّلت في المقياس)، عبر اتصال شبكة حقيقي بقاعدة بيانات Supabase البعيدة (وليس محليًا) — سريع بما يكفي لتجربة "بحث فوري" حتى بجمع 250ms مهلة الـ debounce (الزمن الكلي المُدرَك من المستخدم بعد التوقف عن الكتابة ~300ms).
- **HTTP بثلاثة حسابات حقيقية عبر جلسات Supabase فعلية** (نفس أسلوب `@supabase/ssr` المباشر المُستخدَم في كل الجولات السابقة): حساب طبيب تجريبي، حساب مدير نظام تجريبي (`ADMIN`، أُنشئ خصيصًا لهذه الجولة لعدم وجود حساب تجريبي بهذا الدور مسبقًا)، وحساب موظف الاستقبال التجريبي الموجود مسبقًا — الثلاثة ← **200** على `/admin` والمحتوى يحوي فعليًا زر البحث ("بحث سريع...") وتلميح الاختصار ("Ctrl K") في HTML المُستلَم. كل المسارات السابقة (12 مسارًا) بقيت **307** لزائر غير مسجّل دخول، ولا كسر لأي مسار سابق.
- تم حذف كل الحسابات التجريبية بالكامل بعد الانتهاء (Supabase Auth + صفوف `Staff`)؛ لا بيانات اختبار متبقية.
- **لم يُختبر بصريًا في متصفح حقيقي** (لا Playwright/Puppeteer فعّال متاح): فتح اللوحة فعليًا بالضغط على `Ctrl+K`، التنقّل الفعلي بالأسهم بين النتائج، النقر الفعلي على نتيجة والانتقال، ظهور/مسح "عمليات البحث الأخيرة" بصريًا عبر تحديثات متتالية للمتصفح، شكل التمييز اللوني للنص المطابق (`<mark>`) بصريًا. بدلًا من ذلك، اختير التحقق من: (أ) صحة منطق الصلاحيات والاستعلامات مباشرة على القاعدة الحقيقية، (ب) نجاح الكود الفعلي عبر `tsc`/`eslint`/`build` بما يضمن عدم وجود أخطاء توليف أو نوع تمنع المكوّن من العمل إطلاقًا، (ج) عرض العنصر فعليًا في HTML المُستلَم عبر HTTP حقيقي. هذا نفس حد الاختبار المُعتمَد في كل الوحدات التفاعلية السابقة لهذا المشروع (التقويم، الإشعارات، التقارير) للسبب نفسه: غياب أداة أتمتة متصفح فعّالة في بيئة التطوير الحالية.

### كيف اختُبرت صلاحيات الأدوار الأربعة + حماية الصفحات تحديدًا
- `npx tsc --noEmit`، `npx eslint` على الملفات الثلاثة المعدَّلة (`permissions.ts`, `patients/page.tsx`, `appointments/page.tsx`)، و`npm run build`: نجاح تام بدون أخطاء (لا أخطاء lint جديدة هذه الجولة — تعديل منطقي بحت بلا حالة React تفاعلية).
- **سكربت منطق مباشر مؤقت (33 اختبارًا)** استدعى `hasPermission()` لكل تركيبة (دور × صلاحية) حرِجة للتأكد من الحدود الدقيقة: `ASSISTANT`/`HYGIENIST` يملكان `viewAppointments`/`viewPatients`/`viewMedicalRecord` بالضبط لكن **لا** `editPatient`/`editMedicalRecord`/`viewBilling`/`manageStaff`؛ `ACCOUNTANT` يملك `viewBilling`/`manageInvoices`/`managePayments`/`viewReports`/`viewStatistics` بالضبط لكن **لا** `viewPatients`/`viewAppointments`/`viewMedicalRecord`؛ `ADMIN` لا يزال `manageStaff` فقط؛ `DOCTOR`/`RECEPTIONIST` لم يتأثرا. تحقّق إضافي أن `getVisibleNotificationTypes()` انعكس تلقائيًا لكل دور (مثال: `ASSISTANT` أصبح يرى الآن أنواع إشعارات المواعيد/المرضى/خطط العلاج، `ACCOUNTANT` يرى الفواتير/الدفعات، `ADMIN` بلا تغيير) **بصفر تعديل على `notifications.ts`**. **33/33 نجحت**، ثم حُذف السكربت المؤقت.
- **HTTP بأربعة حسابات حقيقية عبر جلسات Supabase فعلية** (نفس أسلوب `@supabase/ssr` المباشر المُستخدَم في كل الجولات السابقة، حسابات `test-assistant-perm@…`/`test-hygienist-perm@…`/`test-accountant-perm@…`/`test-admin-perm@…` أُنشئت خصيصًا لعدم وجود حسابات تجريبية بهذه الأدوار مسبقًا): كل حساب ← 4 مسارات (`/admin/patients`, `/admin/appointments`, `/admin/reports`, `/admin`) بمنطق `redirect: "manual"` للتقاط حالة 307 دون اتّباعها. **16/16 نتيجة طابقت التوقع بالضبط**: `ASSISTANT`/`HYGIENIST` → `200` على المرضى/المواعيد لكن `307` على التقارير؛ `ACCOUNTANT` → `307` على المرضى/المواعيد لكن `200` على التقارير؛ `ADMIN` → `307` على الثلاثة (لم تتغيّر صلاحياته). لوحة الإدارة الرئيسية `/admin` بقيت `200` للجميع (كلهم مسجّلون دخول بصلاحية ما).
- تم حذف كل الحسابات التجريبية الأربعة بالكامل بعد الاختبار (Supabase Auth + صفوف `Staff`)، وتم التحقق مباشرة من قاعدة البيانات أن لا صفوف موظفين تجريبية متبقية (`email LIKE 'test-%'` → صفر نتائج).
- **لم يُختبر بصريًا في متصفح حقيقي**: تجربة موظف فعلي بأحد الأدوار الأربعة يتصفح لوحة الإدارة (يرى فقط ما يُفترض أن يراه في القوائم/الشريط الجانبي). المنطق الخادمي (الصلاحية نفسها + التحويل) مُختبَر فعليًا كما ورد أعلاه.

### كيف اختُبر الموقع العام ثنائي اللغة تحديدًا
- `npx tsc --noEmit`: فشل أولًا بخطأ حقيقي (تضييق نوع حرفي بسبب `as const` في قاموس العربية جعل قاموس الإنجليزية غير متوافق نوعيًا) — أُصلح بإزالة `as const` (القيم تُوسَّع تلقائيًا إلى `string` دون الحاجة له، المفاتيح تبقى مضبوطة النوع بما يكفي). بعدها **صفر أخطاء**. تم اكتشاف مشكلة حقيقية ثانية أثناء `npm run build`: عشرات الملفات في `admin/` كانت تستورد أكواد الإجراءات عبر مسارات مطلقة `@/app/admin/.../actions` (قبل نقل `admin` إلى `(admin)` هذه الجولة) — أُصلحت جميعها ببحث واستبدال دقيق لكل الملفات الـ36 المتأثرة (`@/app/admin/` → `@/app/(admin)/admin/`، `@/app/login/` → `@/app/(admin)/login/`)، تم التحقق أن الاستبدال طبَّق على الجميع (`grep` بعده أعاد صفر تطابقات قديمة).
- `npx eslint src`: خطأ حقيقي واحد اكتُشف وأُصلح — `react-hooks/immutability` رفض `document.cookie = ...` مباشرة داخل جسم مكوّن `LanguageSwitcher` (نفس عائلة القاعدة التي ظهرت في جلسة البحث الشامل بشكل مختلف)، أُصلح بنقل السطر إلى دالة `persistLocaleCookie()` عادية مُعرَّفة خارج المكوّن، وليس بتجاهل القاعدة. التحذير المتبقي الوحيد (`react-hooks/incompatible-library` على `watch()` من `react-hook-form`) موجود مسبقًا في نمط الاستخدام نفسه ولا علاقة له بهذه الجولة (نفس فئة تحذير غير حاجز وُثِّقت سابقًا).
- `npm run build` (بعد حذف `.next` القديم لأن أنواعه المولَّدة أشارت لمسارات ما قبل النقل): نجاح تام. جدول المسارات النهائي أكّد أن `/[lang]`، `/[lang]/appointment` ديناميكيان للموقع العام، وكل مسارات `/admin/*` و`/login` بقيت كما هي حرفيًا.
- **خطأ زمن تشغيل حقيقي اكتُشف وأُصلح أثناء أول build ناجح لاحقًا**: `Error: usePublicI18n must be used within a PublicI18nProvider` عند توليد `/login` مسبقًا (Prerender) — السبب: نسخة `(admin)/layout.tsx` احتفظت بـ`BookingDialogProvider` (الذي يُركِّب `AppointmentForm` دائمًا وإن كانت النافذة مغلقة) من `layout.tsx` القديم، والنموذج أصبح يعتمد على `usePublicI18n()` غير المتاح في شجرة admin. أُصلح بإزالة `BookingDialogProvider` من نسخة admin فقط (تم التحقق أولًا أن لا مكوّن admin يستدعي `useBookingDialog()` إطلاقًا — كان غلافًا ميتًا هناك أصلًا، فإزالته من admin تحديدًا لا تُغيّر أي سلوك ظاهر).
- **HTTP على خادم تطوير حقيقي**: `/` → `200` بـ`<html lang="ar" dir="rtl">` ومحتوى عربي؛ `/appointment` → `200`؛ `/en` → `200` بـ`<html lang="en" dir="ltr">` ومحتوى إنجليزي فعلي ("Book Your Appointment", "Why Choose"...)؛ `/en/appointment` → `200`؛ طلب `/` بكوكي `NEXT_LOCALE=en` ← **307** إلى `/en` فعليًا (تفضيل محفوظ يعمل)؛ وسوم `hreflang` (`ar`/`en`/`x-default`) ظهرت فعليًا في HTML لكل من الصفحة الرئيسية وصفحة الحجز، مع عنوان/وصف مختلفين لكل لغة.
- **انحدار admin كامل (18 فحصًا) بجلستي Supabase حقيقيتين** (حساب طبيب تجريبي جديد + حساب موظف الاستقبال التجريبي الموجود مسبقًا، نفس أسلوب `@supabase/ssr` المباشر المُستخدَم في كل الجولات): كل مسارات admin العشرة (`/admin`, `/appointments`, `/patients`, `/billing`, `/reports`, `/staff`, `/doctors`, `/activity`, `/notifications`, `/settings/clinic`) ← **200** لحساب الطبيب؛ موظف الاستقبال ← **200** على المرضى/المواعيد/الفوترة لكن **307** على الموظفين/الأطباء (كما كان تمامًا)؛ `/login` لحساب مسجّل دخول ← **307**؛ `/admin` لزائر مجهول ← **307**. **18/18 نجحت** — إثبات فعلي (وليس افتراضًا) أن إعادة الهيكلة لم تُغيّر أي سلوك admin. تم حذف الحساب التجريبي بالكامل بعد الاختبار.
- **حجز حقيقي كامل باللغة الإنجليزية** (استدعاء مباشر لـ`createAppointment` الفعلية بمعامل `locale="en"`، عبر `npx tsx --conditions=react-server` لتجاوز قيد حزمة `server-only` خارج بيئة بناء Next.js فقط — لا تغيير في الكود نفسه): محاولة أولى في يوم مغلق (جمعة) أعادت رسالة تحقق **إنجليزية** صحيحة ("The clinic is closed on Fridays...")، محاولة ثانية في يوم مفتوح نجحت (`{success:true}`)، مريض وموعد حقيقيان أُنشئا في قاعدة البيانات فعليًا، **وإشعارا الإدارة الناتجان ("موعد جديد"، "مريض جديد") بقيا عربيَّين بالكامل** رغم أن الحجز كان بالإنجليزية — يثبت فعليًا القيد المذكور أعلاه أن إشعارات admin لا تتأثر بلغة الزائر العام. تم حذف كل بيانات الاختبار (مريض/موعد/إشعارين) بعد التحقق.
- تم التحقق من عدم وجود أي بيانات أو سكربتات اختبار متبقية (`scripts/_tmp-*.ts` كلها حُذفت فور الانتهاء منها، صفر صفوف `Staff`/`Patient` تجريبية متبقية في قاعدة البيانات).
- **لم يُختبر بصريًا في متصفح حقيقي** (لا أداة أتمتة متصفح فعّالة متاحة في هذه الجلسة، وتم عرض تثبيت إضافة Chrome على المستخدم فاختار المتابعة بدونها): الانعكاس البصري الفعلي بين RTL/LTR (خاصة الشارات العائمة المُزاحة في hero/about-doctor/treatment-steps/contact بعد استبدال أصنافها بمكافئات منطقية)، تجاوب الموبايل الفعلي، سلوك مبدّل اللغة بالنقر الفعلي. بدلًا من ذلك اعتُمد نفس حد الاختبار المُتَّبع في كل الوحدات التفاعلية السابقة لهذا المشروع: التحقق من `dir`/`lang` والنص الفعلي في HTML المُستلَم عبر HTTP حقيقي، ونجاح `tsc`/`eslint`/`build` الكامل.

---

## 🏗️ البنية المعمارية الحالية (Current Architecture)

- **الإطار**: Next.js 16.2.12 (App Router + Turbopack)، React 19.2.4. ⚠️ **نسخة Next.js هذه بها تغييرات جذرية عن المعتاد** — راجع `node_modules/next/dist/docs/` قبل افتراض أي سلوك (مثال حقيقي حدث فعلًا: `middleware.ts` أصبح اسمه `proxy.ts`).
- **قاعدة البيانات**: PostgreSQL عبر Supabase، ORM عبر Prisma 7.9.0 مع **Driver Adapter** إلزامي (`@prisma/adapter-pg`) — لا يعمل `PrismaClient` بدون تمرير `adapter` صراحة (راجع `src/lib/prisma.ts`).
- **المصادقة**: Supabase Auth حقيقي. جدول `Staff` في Postgres يعكس (mirror) مستخدمي `auth.users` بنفس الـ `id`. الحماية على مستويين: `src/proxy.ts` (تحويل مبكر) + `getCurrentStaff()` في كل صفحة/إجراء خادم.
- **الصلاحيات (RBAC)**: نظام صلاحيات مركزي في `src/lib/auth/permissions.ts` — دالة `hasPermission(role, permission)` و`assertPermission(role, permission)`. كل صفحة/Server Action تتحقق يدويًا (لا Middleware عام واحد يغطي كل شيء — راجع الفجوة المذكورة في "قيود معروفة").
- **التخزين**: Supabase Storage، bucket خاص `patient-files` (ملفات المرضى)، bucket عام `clinic-assets` (شعارات + صور هوية العيادة + صور الموظفين الشخصية تحت `staff-avatars/`).
- **التحقق من صحة البيانات**: Zod في `src/lib/validations/*.ts` لكل نموذج (appointment, clinic-settings, staff, medical-record...).
- **الواجهة**: Tailwind CSS v4، مكوّنات UI مبنية على `@base-ui/react` (بأسلوب shadcn، محلية في `src/components/ui/`)، `framer-motion` للحركات، `recharts` للرسوم البيانية، `next-themes`، `sonner` للتنبيهات، خط Cairo (عربي/لاتيني). **لوحة الإدارة**: RTL ثابت دائمًا (`dir="rtl"`، عربي فقط، لم يتغيّر). **الموقع العام**: RTL/LTR ديناميكي حسب اللغة المُختارة (`dir="rtl"` للعربية الافتراضية، `dir="ltr"` للإنجليزية عبر `/en`) — راجع وحدة "الموقع العام ثنائي اللغة" أعلاه.
- **الأرقام والتواريخ**: `Intl.NumberFormat`/`Intl.DateTimeFormat` بلغة `ar-SY` — يعرض أرقامًا هندية شرقية (١٢٣) عمدًا (قرار موروث من مرحلة التثبيت، **لا تُغيّره بدون تأكيد صريح من المستخدم**). هذا لا يزال صحيحًا **للوحة الإدارة فقط**؛ الموقع العام الإنجليزي يستخدم أرقامًا/تنسيقًا إنجليزيًا عاديًا في نصوصه الثابتة.
- **⚠️ i18n للموقع العام فقط، ومكتبة `server-only` لا تعمل خارج بناء Next.js**: القاموس والتوجيه في `src/lib/i18n/` مبنيان يدويًا (لا `next-intl`). أي محاولة لاستدعاء ملف يستورد `server-only` (مثل `get-dictionary.ts` أو أي Server Action) مباشرة من سكربت `tsx` مستقل يفشل بخطأ حقيقي ("This module cannot be imported from a Client Component module") لأن Next.js فقط هو من يستبدل الحزمة بنسخة فارغة عبر شرط تصدير `react-server` أثناء البناء. **الحل المُستخدَم فعليًا**: تشغيل السكربت عبر `npx tsx --conditions=react-server script.ts` (يُمرَّر مباشرة لـ Node)، وليس تعديل الكود نفسه.
- **هيكل المجلدات الأساسي**:
  ```
  src/app/
    (admin)/                  → مجموعة مسارات (route group، لا تظهر في الرابط) لكل ما هو عربي بحت
      admin/                  → لوحة الإدارة (كل صفحاتها تستدعي getCurrentStaff())
      login/
      layout.tsx              → Root layout خاص بها (نسخة طبق الأصل من layout.tsx القديم، lang="ar" dir="rtl" ثابت)
    (public)/[lang]/          → الموقع العام ثنائي اللغة (route group + segment ديناميكي [lang])
      page.tsx                → الصفحة الرئيسية
      appointment/            → صفحة الحجز + actions.ts
      layout.tsx              → Root layout خاص بها (lang/dir ديناميكيان حسب [lang])
    globals.css, favicon.ico  → يبقيان على مستوى app/ الجذري (خارج أي route group)، يخدمان كل المجموعات
  src/components/
    ui/                       → مكوّنات أساسية (button, dialog, select, avatar...)
    admin/                    → مكوّنات خاصة بلوحة الإدارة (staff/, patients/, billing/, settings/...)
    home/, layout/, appointment/ → الموقع العام (تستهلك dict عبر usePublicI18n())
    public-i18n-provider.tsx  → Context مطابق تمامًا لنمط ClinicSettingsProvider، يوصّل القاموس للمكوّنات العميلة
  src/lib/
    i18n/                     → config.ts, get-dictionary.ts, dictionaries/{ar,en}.ts (جديد)
    auth/                     → permissions.ts, current-staff.ts
    validations/              → Zod schemas
    supabase/                 → server.ts, admin.ts (service role), middleware.ts
    prisma.ts, staff-roles.ts, activity-log.ts, date-utils.ts...
  src/generated/prisma/       → عميل Prisma المولَّد (لا تُعدّله يدويًا؛ يُولَّد عبر `npx prisma generate`)
  prisma/schema.prisma        → المخطط + migrations/
  scripts/                    → bootstrap-doctor.ts, setup-storage.ts (tsx)
  ```
- **⚠️ فخ مسارات import مطلقة عند نقل مجلدات داخل `src/app/`**: أي استيراد بصيغة `@/app/<مسار>` يتبع **الموقع الفعلي على القرص**، وليس رابط URL النهائي. نقل `admin`/`login` إلى `(admin)/admin`/`(admin)/login` هذه الجولة (لأجل route group منفصل) كسر فعليًا **36 ملفًا** يستوردون `actions.ts` الخاص بصفحاتهم عبر `@/app/admin/.../actions` القديم — اكتُشف عبر `tsc --noEmit` مباشرة بعد النقل، وأُصلح ببحث/استبدال دقيق لكل الملفات. **قاعدة للمستقبل**: أي إعادة تسمية/نقل لمجلد داخل `src/app/` يتطلب `grep` شامل عن `@/app/<الاسم_القديم>` عبر `src/` بالكامل قبل اعتبار النقل مكتملًا.
- **تقويم المواعيد** (`src/components/admin/appointments/calendar/`) مبني بالكامل يدويًا بدون أي مكتبة تقويم خارجية (لا FullCalendar، لا dnd-kit) — السحب والإفلات عبر HTML5 Drag and Drop API الأصلي، تغيير المدة عبر أحداث Mouse يدوية، وحساب الشبكات الزمنية عبر `src/lib/calendar-utils.ts` و`src/lib/calendar-working-hours.ts` الجديدين. هذا قرار متعمَّد لتفادي إضافة تبعية ثقيلة جديدة وصعوبة تطويع تصميمها لنظام الألوان/RTL الحالي — إن احتجت مستقبلًا ميزات تقويم أعقد (Resource view حقيقي لعدة أطباء جنبًا إلى جنب، تكرار مواعيد دوري...) قد يستحق التقييم مقابل مكتبة جاهزة عندئذٍ.
- **Supabase Realtime مُفعَّل الآن فعليًا** (أول استخدام له في المشروع) لجدول `Notification` فقط حتى الآن — عبر `src/lib/supabase/client.ts` (كان موجودًا مسبقًا بدون استخدام فعلي) و`src/components/admin/notifications/use-notification-refresh.ts` الجديد. **RLS مُفعَّلة على جدول `Notification` فقط** من بين كل جداول المشروع (بقية الجداول بلا RLS كالمعتاد، الحماية بالكامل من طبقة Next.js). سياسة القراءة صريحة الإتاحة لكل مستخدم `authenticated` (تصفية الظهور حسب الدور تتم بالكامل في طبقة التطبيق، ليس عبر RLS — راجع migration الخاص بها للتفاصيل والتبرير).
- **نمط جديد**: `src/lib/create-notification.ts` (server-only، يُستدعى من داخل Server Actions أخرى) مقابل `src/lib/notifications.ts` (بلا `server-only`، ثوابت/دوال آمنة للعميل والخادم معًا) — **فصل متعمَّد** لأن ثوابت التسميات/الأيقونات يجب أن تُستورَد من مكوّنات عميل (الجرس، عناصر القائمة)، بينما الكتابة الفعلية لقاعدة البيانات يجب أن تبقى محصورة بالخادم. إن أضفت شيئًا جديدًا يخلط استيراد Prisma مع ثوابت مشتركة للعميل، افصلهما بنفس الطريقة.
- **⚠️ فخ بيئة تطوير مهم**: `src/lib/prisma.ts` يستخدم نمط singleton عبر `globalThis` لينجو من HMR أثناء التطوير. هذا يعني أنه **بعد أي `prisma migrate` أو `prisma generate`، يجب إعادة تشغيل خادم `next dev` يدويًا** — وإلا يستمر الخادم باستخدام عميل Prisma قديم في الذاكرة ويفشل بخطأ مثل `Value 'X' not found in enum` حتى لو كانت الملفات على القرص محدَّثة. **حدث هذا فعليًا في هذه الجلسة** وتم حله بإعادة تشغيل الخادم.
- **`src/lib/reports.ts`** (جديد، `server-only`): طبقة بيانات مركزية للتقارير — `resolveReportRange()` (يحوّل فلتر الفترة إلى `{from, to}` فعليَّين) و`getReportsData()` (كل استعلامات البطاقات/الرسوم في دفعة واحدة عبر `Promise.all`، بأقل عدد ممكن من الاستعلامات) و`buildExportTables()` (يُسطِّح النتيجة لجداول تصدير). **أول استخدام لـ`prisma.$queryRaw`** في المشروع (لتجميع الاتجاهات الشهرية عبر `date_trunc`، بدل حلقة استعلامات لكل شهر كما كان في النمط القديم) — مُعامَل بأمان عبر `Prisma.sql`/`Prisma.empty` (بلا أي دمج نصي مباشر لمدخلات المستخدم).
- **مكتبة `xlsx` (SheetJS) جديدة** لتصدير Excel حقيقي — **تحمل ثغرتين معروفتين غير مُصلَحتين على npm** (Prototype Pollution، ReDoS)، كلتاهما في مسار قراءة/تحليل ملفات خارجية فقط؛ الكود هنا يستخدم مسار الكتابة حصرًا (بيانات من الخادم نفسه، لا رفع ملفات). **تم عرض هذا صراحة على المستخدم وتأكيد المتابعة** — إن ظهر هذا في `npm audit` مستقبلًا فهو معروف ومقبول، وليس اكتشافًا جديدًا يستدعي القلق.
- **⚠️ فخ جديد اكتُشف هذه الجلسة**: **لا يمكن تمرير دالة (function) كخاصية (prop) من مكوّن خادم (Server Component) إلى مكوّن عميل (`"use client"`)** في Next.js App Router — يفشل فورًا بخطأ 500 وقت التشغيل ("Functions cannot be passed directly to Client Components") رغم نجاح `tsc`/`npm run build` الساكن تمامًا (لا يُكتشف إلا عند التشغيل الفعلي). الحل المُطبَّق: بدل تمرير `valueFormatter={(v) => formatCurrency(v)}`، مرّر خاصية نصية بسيطة (`valueFormat: "currency" | "number"`) ودَع المكوّن العميل يُنفِّذ التنسيق داخليًا بنفسه. **قاعدة عامة للمستقبل**: أي رسم بياني جديد يحتاج تنسيقًا مخصَّصًا للقيم يجب أن يستقبل إما بيانات مُنسَّقة مسبقًا كنص من الخادم، أو خاصية بدائية (نص/رقم/بوليان) يُفسِّرها هو داخليًا — أبدًا دالة عبر الحدود.
- **⚠️ درس منهجي مهم اكتُشف في بداية جلسة البحث الشامل**: قبل البدء بأي "وحدة جديدة" يطلبها المستخدم، **ابحث فعليًا في الكود عن اسم المكوّن/الميزة المطلوبة أولًا** (`grep`/`find`) — وُجد فعليًا مكوّن `global-search.tsx` جزئي يعمل بالفعل (مستورَد في `admin-shell.tsx`) من جولة لم تُوثَّق في `PROJECT_STATUS.md`/`NEXT_TASK.md` إطلاقًا، وكان يحوي ثغرة صلاحية حقيقية (بحث مرضى بلا أي فحص `viewPatients`). لولا هذا البحث المسبق لكان من المحتمل بناء مكوّن مكرِّر بجانبه بدل استبداله بالكامل. **قاعدة عامة**: عدم وجود توثيق لميزة في `PROJECT_STATUS.md` لا يعني عدم وجودها في الكود فعليًا — تحقّق دائمًا.
- **`react-hooks/set-state-in-effect` و`react-hooks/immutability`**: قاعدتا lint (من نفس مجموعة React Compiler التي ظهرت سابقًا مع `react-hooks/incompatible-library`) تمنعان: (1) استدعاء `setState` متزامنًا مباشرة داخل جسم `useEffect` (الحل: مُهيِّئ كسول `useState(() => ...)` للقراءة لمرة واحدة عند التحميل، أو دالة تُستدعى مباشرة من نقطة الحدث الفعلية بدل effect تفاعلي، أو نقل `setState` داخل رد نداء غير متزامن كـ`.then()`/`setTimeout`)، (2) إعادة تعيين متغيّر عادي (`let x؛ x++`) أثناء الترسيم (الحل: `Map`/مصفوفة مبنية مسبقًا عبر `useMemo` بدل عدّاد متغيّر). كلتاهما ظهرتا فعليًا في `global-search.tsx` هذه الجولة وأُصلحتا بإعادة الهيكلة الصحيحة، ليس بالتعطيل.

---

## 🗄️ حالة قاعدة البيانات (Database Status)

- **11 migration**، كلها مُطبَّقة والمخطط متزامن تمامًا مع قاعدة البيانات (تم التحقق عبر `npx prisma migrate status` — "Database schema is up to date!").
- كل migration حتى الآن **إضافي بحت (additive-only)** على مستوى المخطط: لا حذف أعمدة، لا حذف قيم enum، لا إعادة تسمية تُفقد بيانات. الاستثناء الوحيد كان تحويل `openTime/closeTime/closedWeekdays` إلى JSON موحّد `workingHours` في `ClinicSettings` (مرحلة 4)، ونُفِّذ عبر migration يدوي حوَّل البيانات القديمة دون فقدان. **استثناء جديد من نوع مختلف**: آخر migration (`enable_notification_realtime`) تُفعِّل RLS وتُضيف publication — أول migration في المشروع يغيّر شيئًا غير بنية الأعمدة/الـ enums (راجع البند أدناه).
- **النماذج (Models)**: `Patient`, `Doctor` (أضيف إليه عمود `isActive` هذه الجولة), `Appointment`, `Staff`, `ActivityLog`, `ClinicSettings`, `MedicalHistory`, `DentalVisit`, `ToothCondition`, `PatientFile`, `TreatmentPlan`, `TreatmentItem`, `Invoice`, `InvoiceItem`, `Payment`, `Notification`. لا نماذج مكرّرة — إدارة الأطباء أعادت استخدام نموذج `Doctor` الموجود بالكامل (لم يُضَف نموذج جديد، فقط عمود واحد).
- **أهم Enums**: `StaffRole` (6 قيم)، `AppointmentStatus` (6 قيم، بما فيها `NO_SHOW`)، `TreatmentPlanStatus`/`TreatmentItemStatus`، `InvoicePaymentStatus`، `ToothConditionType` (12 حالة)، `Gender`، `SmokingStatus`، `BloodType`، `PatientFileType`، `PaymentMethod`، **`NotificationType`** (10 قيم، جديد)، **`NotificationPriority`** (4 قيم: `LOW`/`MEDIUM`/`HIGH`/`CRITICAL`، جديد).
- **آخر migration فعليًا مُطبَّقة لهذه الجولة**:
  1. `20260727004046_notifications_center` — تُنشئ نموذج `Notification` والـ enums الجديدة (إضافي بحت، لا مساس ببيانات موجودة).
  2. `20260727004104_enable_notification_realtime` — **تُفعِّل Row Level Security على جدول `Notification` فقط** (لا جدول آخر في المشروع لديه RLS) + سياسة `SELECT` صريحة لـ`authenticated` + إضافة الجدول إلى نشر `supabase_realtime`. تم التحقق تجريبيًا أن هذا النشر موجود فعلًا على المشروع قبل كتابة الـ migration (وليس افتراضًا). الجملة الخاصة بالنشر محاطة بفحص `IF EXISTS` لأن قاعدة بيانات Prisma shadow (المستخدمة للتحقق من صحة migrations) لا تحتوي هذا النشر أصلًا — بدون هذا الفحص يفشل `prisma migrate dev` عند كل تشغيل مستقبلي.
  3. `20260727145140_add_doctor_is_active` — تُضيف عمود `Doctor.isActive Boolean @default(true)` (إضافي بحت، كل الأطباء الحاليين أصبحوا `true` تلقائيًا).
- **التقارير والتحليلات: صفر migrations جديدة** — أول وحدة في المشروع لا تحتاج أي تعديل على `schema.prisma` إطلاقًا، إعادة استخدام كاملة للجداول الموجودة كما طُلب صراحة.
- **البحث الشامل الذكي: صفر migrations أيضًا** — لا نموذج/عمود جديد؛ البحث يقرأ فقط من الجداول الموجودة (`Patient`, `Appointment`, `TreatmentPlan`, `DentalVisit`, `Invoice`, `Payment`, `Staff`, `Notification`)، و"عمليات البحث الأخيرة" مُخزَّنة بالكامل في `localStorage` للمتصفح (لا جدول قاعدة بيانات).
- **صلاحيات الأدوار الأربعة + حماية الصفحات: صفر migrations** — تعديل منطقي بحت في `permissions.ts` (محتوى مصفوفات ثابتة) + فحص صلاحية في بداية صفحتين موجودتين، لا مساس بالمخطط إطلاقًا.
- **الموقع العام ثنائي اللغة (هذه الجولة): صفر migrations أيضًا** — إعادة هيكلة مسارات + قاموس ترجمة بالكامل على مستوى الكود، لا عمود/نموذج جديد. لا تزال **11 migration** فقط (تم التحقق `npx prisma migrate status` → "Database schema is up to date!" بعد هذه الجولة أيضًا).
- **بيانات تجريبية متبقية عمدًا**: حساب موظف استقبال تجريبي (`receptionist.test@khalil-clinic.com`) — راجع "قيود معروفة" أدناه. لا توجد أي بيانات تجريبية أخرى متبقية (كل حسابات/بيانات الاختبار المؤقتة من كل الجلسات، بما فيها هذه، أُنشئت ثم حُذفت بالكامل).

---

## 🔜 خارطة الطريق المتبقية بالترتيب (Remaining Roadmap, in order)

الترتيب أدناه يعكس التبعية المنطقية والأولوية الفعلية، وليس مجرد قائمة غير مرتبة. **ملاحظة**: "الموقع العام ثنائي اللغة" (آخر ما أُنجز) لم يكن على هذه القائمة أصلًا — طُلب مباشرة من المستخدم خارج الترتيب (حقه بالطبع)، فبقيت القائمة أدناه كما كانت.

1. **ربط محرك حجز المواعيد العام (الموقع العام) فعليًا بساعات الدوام** المُعدَّلة من صفحة الإعدادات — تقويم لوحة الإدارة يقرأها فعليًا، لكن **نموذج الحجز العام على الموقع الرئيسي لا يزال منفصلًا** ويستخدم قيمًا ثابتة (`appointment-slots.ts`)، كما كان موثَّقًا سابقًا. **← هذا هو الموصى به تاليًا، راجع `NEXT_TASK.md`.**
2. **صفحة "تغيير كلمة المرور" الذاتية** للموظف من داخل حسابه (تختلف عن إعادة التعيين الإدارية الموجودة).
3. **تنزيل PDF مباشر** للفواتير (مكتبة توليد PDF من الخادم) بدل خيار الطباعة الحالي.
4. **ربط الفواتير بالتأمين الصحي** إذا كانت العيادة تتعامل معه.
5. **قوالب بريد إلكتروني ديناميكية** (تأكيد الحجز...) تستخدم بيانات `ClinicSettings`.
6. **إعادة النظر في نظام الأرقام** (هندي شرقي مقابل لاتيني) — فقط إذا أكّد المستخدم أن الاختيار الحالي غير مقصود.
7. **دمج خطط العلاج بصريًا في التقويم** — الطلب الحالي اقتصر على "استبدال قائمة المواعيد بتقويم"؛ خطط العلاج لا تظهر في هذا التقويم بعد (تبقى في `/admin/patients/[id]` كما كانت).
8. **صفحة تغيير كلمة مرور ذاتية + إشعار مرتبط بها** إن أُضيفت مستقبلًا (البند 2) — يستحق التفكير حينها في إشعار "STAFF_CREATED"-مثيل جديد لتغيير كلمة المرور الذاتي، إن رغب المستخدم.
9. **إشعارات لكل مستخدم بدل حالة قراءة عامة** — راجع القيد رقم 13 أدناه؛ ترقية مستقبلية إن ثبت أن حالة "مقروء" العامة الحالية غير كافية عمليًا لفريق أكبر.
10. **ربط إدارة الأطباء بمركز الإشعارات** (إن رغبت العيادة) — إضافة/تعديل/تعطيل طبيب لا يُولِّد إشعارًا حاليًا (قرار مقصود، لم يُطلب هذه الجولة)، يتطلب قيمة enum جديدة + migration.

---

## ⚠️ قيود معروفة (Known Issues / Simplifications)

1. **لا توجد صفحة "تغيير كلمة المرور" ذاتية للموظف** — الموجود حاليًا هو إعادة تعيين كلمة مرور **موظف آخر** من قِبَل صاحب صلاحية `manageStaff` (طبيب/مدير النظام)، وليس تغيير الموظف كلمة مروره الخاصة بنفسه.
2. ~~الإشعارات عبر polling كل 20 ثانية فقط~~ — **تم حله**: مركز الإشعارات الجديد يستخدم Supabase Realtime حقيقيًا (مُختبَر فعليًا) مع Polling كل 30 ثانية كشبكة أمان إضافية بجانبه، وليس بديلًا.
3. **تصدير PDF** عبر "حفظ كـ PDF" في نافذة طباعة المتصفح (`@media print`)، لا توليد PDF من الخادم — ينطبق الآن على الفواتير **والتقارير** (نفس القرار والتقنية بالضبط، امتداد متعمَّد للنمط الموجود بعد تأكيد صريح من المستخدم).
4. **الأرقام والتواريخ بالصيغة الهندية الشرقية** (١٢٣) — قرار مقصود من مرحلة التثبيت، لا تُغيّره بدون تأكيد صريح. **يخص لوحة الإدارة والنسخة العربية من الموقع العام فقط الآن** (منذ جولة الموقع ثنائي اللغة)؛ النسخة الإنجليزية للموقع العام (`/en`) تستخدم أرقامًا/نصوصًا إنجليزية عادية في محتواها الثابت.
5. **محرك حجز المواعيد العام (الموقع العام) لم يُربط بساعات الدوام القابلة للتعديل** — تعديل الساعات من الإعدادات يُحدِّث العرض على الصفحة الرئيسية فقط، وليس منطق الفتحات المتاحة فعليًا في `appointment-slots.ts` المستخدم في نموذج الحجز العام. **ملاحظة**: تقويم لوحة الإدارة الجديد (`/admin/appointments`) **يقرأ ساعات الدوام فعليًا** من `ClinicSettings` عبر ملف منفصل جديد (`calendar-working-hours.ts`) ويمنع السحب/الإفلات/الإنشاء خارجها — لكن هذا لا يشمل نموذج الحجز العام، وهذا يعني الآن وجود **مصدرين منفصلين** لمنطق ساعات الدوام في المشروع (قديم للموقع العام، جديد للتقويم الإداري). يُنصح بتوحيدهما لاحقًا إن رغبت.
6. **قوالب البريد الإلكتروني** غير موجودة أصلًا في التطبيق (لا بنية بريد إلكتروني بعد، تم التحقق فعليًا مجددًا هذه الجولة — لا `nodemailer`/`resend`/أي مكتبة بريد في `package.json` أو الكود). لذلك "رسائل تأكيد الحجز بلغة الزائر" (المطلوبة في جولة الموقع ثنائي اللغة) طُبِّقت فقط كرسالة نجاح/خطأ داخل الصفحة (toast) بلغة الزائر — لا بريد إلكتروني فعلي يُرسَل حاليًا لأي حجز، بأي لغة.
7. **حساب موظف استقبال تجريبي متبقٍ عمدًا** (يمكن حذفه من `/admin/staff` متى رغبتَ):
   - البريد: `receptionist.test@khalil-clinic.com` / كلمة المرور: `1VFcFQgeDHRD`
8. ~~صلاحيات الأدوار الأربعة الجديدة (Admin/Assistant/Hygienist/Accountant) محدودة عمدًا~~ — **تم حله**: `ASSISTANT`/`HYGIENIST` (اطّلاع سريري فقط)، `ACCOUNTANT` (فوترة وتقارير فقط)، `ADMIN` (بلا تغيير، حسب اختيار المستخدم صراحة). راجع تفاصيل الوحدة أعلاه.
9. ~~`/admin/patients` و`/admin/appointments` بلا حماية صلاحية على مستوى الصفحة نفسها~~ — **تم حله**: كلتاهما الآن تُحوِّلان (307) لمن لا يملك `viewPatients`/`viewAppointments` على التوالي، بنفس نمط `/admin/staff`/`/admin/billing`.
10. **فخ بيئة التطوير**: عميل Prisma singleton (`globalForPrisma`) يبقى في الذاكرة عبر HMR — أي تعديل على `schema.prisma` يتطلب إعادة تشغيل `next dev` يدويًا بعد `prisma generate`/`migrate`، وإلا ستظهر أخطاء enum/عمود غير موجود رغم أن الملفات محدَّثة على القرص. **تنبيه إضافي اكتُشف هذه الجولة**: العميل المولَّد نفسه (`src/generated/prisma`) يبقى قديمًا أيضًا بعد `prisma migrate dev` إن لم يُشغَّل `prisma generate` بعده صراحة (عادة يُشغَّل تلقائيًا ضمن `migrate dev`، لكن حدث فعليًا أن `tsc` أظهر أخطاء "Property does not exist" على حقل جديد حتى بعد الـ migration — الحل: `npx prisma generate` يدويًا ثم إعادة تشغيل `next dev`).
11. **عدم تناسق متعمَّد في تطبيق ساعات الدوام**: إنشاء موعد يدوي من لوحة الإدارة (`createManualAppointment`، الحوار القديم "حجز جديد") **لا يزال لا يتحقق من ساعات الدوام** (سلوك موجود مسبقًا، لم يُغيَّر حفاظًا على عدم كسر مرونة الإدخال اليدوي الحالية)، بينما **نقل/تمديد موعد عبر التقويم الجديد بالسحب** (`rescheduleAppointment`) **يرفض ذلك فعليًا**. هذا قرار واعٍ لتفادي تعديل سلوك موجود ومُختبَر، وليس تناقضًا غير مقصود — لكن يستحق الانتباه إن بدا غريبًا لاحقًا.
12. **حالة `NO_SHOW` الجديدة** غير مربوطة بعد بأي منطق تلقائي (مثل وضع موعد فائت الوقت كـ"لم يحضر" تلقائيًا) — التحديث حاليًا يدوي فقط من نافذة تفاصيل الموعد في التقويم.
13. **حالة "مقروء" في الإشعارات عامة وليست لكل مستخدم على حدة** — حقل `isRead` واحد لكل إشعار، وليس جدول ربط لكل موظف. بمعنى: إن كان في العيادة أكثر من طبيب واحد يستخدمان نفس التطبيق، وقرأ أحدهما إشعارًا، سيظهر "مقروء" للآخر أيضًا رغم أنه لم يره فعليًا. قرار مبسَّط مقصود التزامًا بتعليمة "الحد الأدنى من المخطط" — مقبول لفريق عيادة صغير نمطي (طبيب واحد + عدد قليل من الموظفين)، لكن يستحق إعادة النظر إن توسّع الفريق لعدة أطباء يستخدمون النظام بالتوازي.
14. **لا حدث حالي مُصنَّف بأولوية "حرجة" (`CRITICAL`) تلقائيًا** — المستوى مدعوم بالكامل في المخطط والفلاتر، لكن لم يُستخدم بعد لأي من الأحداث العشرة الحالية (أعلى أولوية مُستخدمة فعليًا هي "عالية"). يمكن تعيينه لاحقًا لأي حدث تراه العيادة حرجًا فعلًا (فشل دفعة، تعارض حجز خطير...) دون أي تعديل بنيوي.
15. **رفع أولوية "إلغاء موعد" و"موظف جديد" إلى `HIGH` كان قرارًا تقديريًا من المُنفِّذ** (الطلب لم يحدد أولوية كل نوع حدث بالتحديد، فقط طلب دعم 4 مستويات) — إن كانت العيادة ترى ترتيبًا مختلفًا للأهمية، يمكن تعديل ذلك بسطر واحد لكل نوع في نقطة الاستدعاء (لا حاجة لتغيير المخطط أو المنطق).
16. **لا حذف نهائي للطبيب من `/admin/doctors`** — فقط تفعيل/تعطيل (`isActive`)، قرار مقصود لتفادي كسر قيد المفتاح الأجنبي على `Appointment`/`DentalVisit`/`TreatmentPlan`/`Invoice` (كلها `doctorId` إلزامي). إن احتاجت العيادة حذفًا فعليًا لطبيب لم يُستخدم إطلاقًا (صفر مواعيد/فواتير)، يمكن إضافته لاحقًا كإجراء منفصل يتحقق أولًا من عدم وجود أي سجل مرتبط.
17. **إدارة الأطباء لا تتكامل مع مركز الإشعارات بعد** — راجع البند 12 في خارطة الطريق أعلاه.
18. **مكتبة `xlsx` (تصدير Excel في التقارير) تحمل ثغرتين معروفتين غير مُصلَحتين على npm** (Prototype Pollution، ReDoS) — كلتاهما في مسار قراءة/تحليل ملفات خارجية فقط، لا يستخدمه هذا المشروع (الاستخدام هنا كتابة فقط من بيانات الخادم). **تم عرض هذا صراحة على المستخدم وتأكيد المتابعة** — ظهوره في `npm audit` مستقبلًا معروف ومقبول، وليس اكتشافًا جديدًا. إن رغبت لاحقًا بإزالته، البديل الوحيد المطبَّق فعليًا في الكود هو تصدير CSV (يعمل بلا أي مكتبة).
19. **"الإيرادات حسب الخدمة" و"أكثر الإجراءات شيوعًا" في التقارير محدودتان بأعلى 10 عناصر فقط** (`take: 10` في الاستعلام) — قرار مقصود لإبقاء الرسم البياني مقروءًا، غير مذكور صراحة في الطلب الأصلي لكنه قياسي لهذا النوع من الرسوم. إن احتاجت العيادة رؤية القائمة الكاملة، يحتاج الأمر صفحة/جدول منفصل بدل الرسم البياني.
20. **بطاقات "إيرادات اليوم/الشهر/العام" في التقارير مرتبطة بفترات ثابتة دائمًا** (اليوم الفعلي، بداية الشهر الحالي وحتى الآن، بداية العام الحالي وحتى الآن) — **لا تتأثر بفلتر الفترة** (اليوم/الأسبوع/الشهر/العام/مخصص) أعلاها في نفس الصفحة، فقط بفلتر الطبيب. هذا قرار تصميم مقصود (راجع تفاصيل الوحدة أعلاه) لتفادي غموض دلالي، لكنه قد يبدو للوهلة الأولى وكأن الفلتر "لا يعمل" على هذه البطاقات تحديدًا — وهذا صحيح ومقصود، وليس خللًا.
21. ~~البحث الشامل يعتمد على نفس نظام الصلاحيات المحدود للأدوار الأربعة الجديدة~~ — **تحسّن تلقائيًا بصفر تعديل على كود البحث** فور تحديد صلاحيات هذه الأدوار: `ASSISTANT`/`HYGIENIST` يرون الآن المرضى/المواعيد/خطط العلاج/الزيارات/الإشعارات المرتبطة، `ACCOUNTANT` يرى الفواتير/الدفعات/التقارير، `ADMIN` بلا تغيير (يرى "الموظفون" فقط كما كان). مُختبَر ومؤكَّد فعليًا هذه الجولة.
22. **"عمليات البحث الأخيرة" مخزَّنة في `localStorage` فقط** — خاصة بكل متصفح/جهاز على حدة، لا تُزامَن بين الأجهزة ولا تُحفَظ في قاعدة البيانات، وتُفقَد عند مسح بيانات المتصفح. قرار مقصود ("الحد الأدنى من المخطط" لبيانات عرضية لا تحتاج مزامنة)، وليس قيدًا تقنيًا يستدعي حلًا حاليًا.
23. **نتيجة البحث عن "موعد" تنقل إلى عرض يوم التقويم (`/admin/appointments?view=day&date=...`) وليس إلى الموعد نفسه محدَّدًا/مُظلَّلًا** — لا توجد آلية تمييز موعد واحد بعينه في التقويم حاليًا (لم تُبنَ، خارج نطاق هذه الجولة، ولم يُطلَب تعديل التقويم نفسه). المستخدم يصل إلى يوم الموعد الصحيح ويحتاج تحديده يدويًا ضمن ذلك اليوم.
24. **بيانات العيادة الديناميكية تبقى عربية حتى في النسخة الإنجليزية من الموقع العام** (اسم العيادة، اسم/لقب الطبيب، العنوان، الهاتف، ملخّص ساعات الدوام، وصف العيادة) — **قرار مؤكَّد صراحة من المستخدم** قبل البناء، وليس افتراضًا: لا حقل إنجليزي لهذه البيانات في قاعدة البيانات، وصفحة الإعدادات تبقى عربية بلا أي تعديل كما طُلب. يعني هذا أن صفحة `/en` تعرض واجهة إنجليزية بالكامل (عناوين/أزرار/فقرات) لكن اسم العيادة/الطبيب/العنوان/الهاتف تظهر بالعربية داخلها. إن رغبت العيادة مستقبلًا بحقول إنجليزية اختيارية لهذه البيانات، يتطلب الأمر migration إضافي بسيط + تعديل صفحة الإعدادات (نقاش صريح مطلوب أولًا، راجع الوحدة أعلاه).
25. **لم يُختبر الموقع ثنائي اللغة بصريًا في متصفح حقيقي** — عُرض تثبيت إضافة Chrome على المستخدم أثناء هذه الجلسة واختار المتابعة بدونها. الانعكاس البصري الفعلي لـRTL/LTR (الشارات العائمة في hero/about-doctor/treatment-steps/contact بعد استبدال أصنافها المنطقية)، تجاوب الموبايل، وسلوك مبدّل اللغة بالنقر الفعلي — كلها مبنية وفق نفس الأنماط المُختبرة بصريًا في وحدات سابقة، لكن لم تُشاهَد بصريًا لهذه الجولة تحديدًا. التحقق البديل: `dir`/`lang` والنص الفعلي عبر HTML مُستلَم بـ`curl` حقيقي (راجع تفاصيل الاختبار أعلاه).
26. **"الأطباء" في طلب الترجمة الأصلي فُسِّرت كقسم "عن الدكتور" الموجود** (عيادة طبيب واحد، لا قائمة أطباء عامة على الموقع) — **قرار مؤكَّد صراحة من المستخدم**. إن احتاجت العيادة مستقبلًا قسمًا عامًا يعرض عدة أطباء (من نفس بيانات `/admin/doctors`)، هذه ميزة جديدة منفصلة عن الترجمة، تستحق نقاشًا وتخطيطًا خاصًا بها.

---

## 👉 المهمة التالية الموصى بها (Next Recommended Task)

**ربط محرك حجز المواعيد العام (الموقع العام) فعليًا بساعات الدوام** من `ClinicSettings.workingHours`.

**لماذا هذه تحديدًا**: هذا كان أعلى بند متبقٍ في خارطة الطريق عند جولتين ماضيتين على التوالي (طُلبت بدلًا منه صلاحيات الأدوار الأربعة، ثم الموقع ثنائي اللغة — حق المستخدم بالطبع في كل مرة). تقويم لوحة الإدارة يقرأ ساعات الدوام فعليًا (`calendar-working-hours.ts`) ويمنع الحجز خارجها، لكن **نموذج الحجز العام على الموقع الرئيسي لا يزال منفصلًا تمامًا** ويستخدم قيمًا ثابتة قديمة (`appointment-slots.ts`) — ما يعني وجود مصدرين منفصلين لمنطق ساعات الدوام في المشروع حاليًا (راجع القيد رقم 5). **ملاحظة إضافية بعد جولة الموقع ثنائي اللغة**: أي حل لهذا البند يجب أن يراعي أن نموذج الحجز الآن ثنائي اللغة (`getAppointmentFormSchema(dict)`) — أي رسائل خطأ جديدة متعلقة بساعات الدوام يجب أن تُضاف لكلا القاموسين `ar.ts`/`en.ts`.

راجع `NEXT_TASK.md` للتفاصيل الكاملة والخطوات المقترحة.

---

## 🚀 التشغيل محليًا

```bash
npm run dev
```

- الموقع العام (عربي، افتراضي): http://localhost:3000 — الحجز: http://localhost:3000/appointment
- الموقع العام (إنجليزي): http://localhost:3000/en — الحجز: http://localhost:3000/en/appointment (مبدّل اللغة 🇸🇦/🇬🇧 في الشريط العلوي، يحفظ الاختيار في كوكي)
- تسجيل الدخول للوحة الإدارة: http://localhost:3000/login
- الفواتير: http://localhost:3000/admin/billing — التقارير والتحليلات (أُعيد بناؤها بالكامل): http://localhost:3000/admin/reports
- إعدادات العيادة: http://localhost:3000/admin/settings/clinic
- إدارة الموظفين: http://localhost:3000/admin/staff (طبيب/مدير النظام فقط)
- إدارة الأطباء: http://localhost:3000/admin/doctors (نفس صلاحية `manageStaff` — طبيب/مدير النظام فقط)
- المواعيد (تقويم — الافتراضي الجديد): http://localhost:3000/admin/appointments — القائمة القديمة: `?mode=list`
- الإشعارات: http://localhost:3000/admin/notifications (جرس الإشعارات في الشريط العلوي متاح لكل الأدوار، بمحتوى مُصفَّى حسب صلاحيات كل دور)
- البحث الشامل: `Ctrl+K` (أو `⌘K`) من أي صفحة إدارة، أو زر "بحث سريع..." في الشريط العلوي — 8 فئات، نتائج مُصفَّاة حسب صلاحيات الدور
- إنشاء حساب طبيب جديد: `npm run bootstrap:doctor -- <email> <password> "<الاسم الكامل>"`
- إنشاء bucket تخزين ملفات المرضى (نُفّذت مرة واحدة بالفعل): `npm run setup:storage`
