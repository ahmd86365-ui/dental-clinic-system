# المهمة التالية — نقطة الاستكمال المباشرة

آخر تحديث: 2026-07-28 (جلسة الموقع العام ثنائي اللغة — عربي/إنجليزي)
هذا الملف مخصص لجلسة Claude القادمة كي تُكمل العمل **فورًا دون إعادة تحليل المشروع من الصفر**. للسياق الكامل والتاريخي راجع `PROJECT_STATUS.md`.

---

## 1) أين توقفنا بالضبط (Exactly Where We Stopped)

- آخر وحدة مُنجَزة بالكامل: **الموقع العام ثنائي اللغة (عربي/إنجليزي)** — طُلبت مباشرة من المستخدم (لم تكن على خارطة الطريق). **لوحة الإدارة بقيت عربية بالكامل ولم تُمس إطلاقًا** — تحقُّق فعلي بـ18 فحص HTTP بجلستي طبيب/موظف استقبال حقيقيتين، صفر كسر.
- **ما تغيّر فعليًا**:
  - الموقع العام (الصفحة الرئيسية + الحجز) فقط أصبح ثنائي اللغة. العربية تبقى بلا بادئة في الرابط (`/`, `/appointment` — **لا كسر لأي رابط قديم**)، الإنجليزية ببادئة `/en` (`/en`, `/en/appointment`).
  - `src/app/` أُعيد هيكلته إلى مجموعتي مسارات (route groups): `src/app/(admin)/` (يحوي `admin/` و`login/` كما هما تمامًا، بـ`layout.tsx` جذري جديد لكنه **نسخة طبق الأصل حرفيًا** من القديم) و`src/app/(public)/[lang]/` (الصفحة الرئيسية + الحجز، بـ`layout.tsx` جذري جديد بـ`lang`/`dir` ديناميكيين).
  - `src/proxy.ts`: منطق `updateSession` القديم لـ`/admin`/`/login` **لم يتغيّر حرفيًا**، فقط أُضيف فرع جديد بجانبه لإعادة كتابة/تحويل المسارات العامة حسب اللغة (rewrite داخلي شفاف للعربية، redirect صريح للإنجليزية حسب كوكي `NEXT_LOCALE`).
  - قاموس ترجمة يدوي كامل (لا مكتبة خارجية) في `src/lib/i18n/dictionaries/{ar,en}.ts` + `PublicI18nProvider`/`usePublicI18n()` (نفس نمط `ClinicSettingsProvider` تمامًا) لتوصيله للمكوّنات.
  - **بيانات العيادة الديناميكية (اسم/عنوان/هاتف/ساعات دوام/اسم الطبيب) تبقى عربية دائمًا حتى في `/en`** — قرار مؤكَّد صراحة من المستخدم (لا حقل إنجليزي في قاعدة البيانات، صفحة الإعدادات لم تُمس).
  - نموذج الحجز (`AppointmentForm` + `validations/appointment.ts` + `actions.ts`) أصبح ثنائي اللغة بالكامل (تسميات، رسائل تحقق Zod، رسائل النجاح/الخطأ)، لكن **إشعارات الإدارة الناتجة عن الحجز تبقى عربية ثابتة دائمًا بغض النظر عن لغة الزائر** (قرار متعمَّد، مُختبَر فعليًا).
- **⚠️ درسان مهمّان جدًا اكتُشفا وأُصلحا أثناء هذه الجلسة، احذر تكرارهما**:
  1. **نقل مجلد داخل `src/app/` يكسر كل استيراد مطلق `@/app/<الاسم_القديم>/...` يشير إليه** — نقل `admin`/`login` إلى `(admin)/admin`/`(admin)/login` كسر 36 ملفًا فعليًا (كل ملف يستورد `actions.ts` الخاص بصفحته عبر مسار مطلق). اكتُشف عبر `tsc --noEmit` مباشرة، وأُصلح ببحث/استبدال شامل. **قبل أي نقل مستقبلي لمجلد داخل `app/`، شغّل `grep -rn "@/app/<الاسم_القديم>" src/` أولًا** وتوقَّع الحاجة لإصلاح كل نتيجة.
  2. **`BookingDialogProvider` كان يُركَّب دائمًا (حتى لو كانت نافذته مغلقة) في كل مكان يُستخدَم فيه** — كان "غلافًا ميتًا" غير مؤذٍ في شجرة admin (لا مكوّن admin يفتحه)، لكن بعد أن أصبح `AppointmentForm` بداخله يعتمد على `usePublicI18n()` (غير متاح في admin)، سبّب فشل build/prerender لـ`/login` فعليًا. أُصلح بإزالته من نسخة `(admin)/layout.tsx` فقط (تم التحقق أولًا أنه غير مستخدَم هناك إطلاقًا قبل الحذف).
- **`npm run build`, `npx tsc --noEmit`, `npx eslint src` كلها ناجحة تمامًا** آخر مرة تم التحقق (بعد حذف `.next` القديم يدويًا لأن أنواعه المولَّدة كانت تشير لمسارات ما قبل النقل — **إن ظهر خطأ `Cannot find module '.../page.js'` غريب من `.next/types/`، احذف `.next` وأعد البناء أولًا قبل تشخيص أي شيء آخر**).
- **صفر migrations** — كل هذه الجولة على مستوى الكود فقط، لا مساس بـ`schema.prisma`. لا تزال 11 migration.
- تم اختبار الوحدة عبر: (أ) HTTP لتوجيه اللغة (`/`, `/appointment`, `/en`, `/en/appointment`, تفضيل كوكي `NEXT_LOCALE`) — كلها كما هو متوقَّع، (ب) انحدار admin كامل (18 فحصًا، جلستا Supabase حقيقيتان)، (ج) حجز حقيقي كامل باللغة الإنجليزية (نجح، رسائل التحقق/النجاح إنجليزية، وإشعارا الإدارة الناتجان بقيا عربيَّين كما هو مطلوب)، (د) hreflang/العنوان/الوصف لكل لغة من HTML فعلي. **لم يُختبر بصريًا في متصفح حقيقي** (عُرض تثبيت إضافة Chrome على المستخدم واختار المتابعة بدونها هذه الجولة).
- خادم التطوير (`next dev`) **أُوقف عمدًا** في نهاية الجلسة. شغّله بـ`npm run dev` عند البدء. لم تُلمَس `schema.prisma`، فلا حاجة لإعادة تشغيل قسري بسبب فخ Prisma singleton (لكن تحقق دائمًا).
- لا توجد أي ملفات أو حسابات تجريبية متبقية من اختبار هذه الجلسة (كل `scripts/_tmp-*.ts` حُذفت فور الانتهاء منها، تم التحقق مباشرة من قاعدة البيانات: صفر صفوف تجريبية). الشيء الوحيد المتبقي عمدًا هو حساب موظف استقبال تجريبي واحد (بيانات الدخول في `PROJECT_STATUS.md`).

---

## 2) ما الذي يجب بناؤه تاليًا (What Should Be Built Next)

### المهمة الموصى بها: ربط محرك حجز المواعيد العام بساعات الدوام

هذا كان التوصية عبر جولتين متتاليتين الآن (طُلبت بدلًا منها صلاحيات الأدوار، ثم الموقع ثنائي اللغة — حق المستخدم بالطبع). لا تغيّر جوهريًا بهذه الجولة، فقط أصبح هناك اعتبار إضافي واحد يجب مراعاته:

**السياق**: يوجد **مصدران منفصلان** لمنطق ساعات الدوام:
- **الجديد** (`src/lib/calendar-working-hours.ts`): يقرأ فعليًا من `ClinicSettings.workingHours`، يُستخدَم فقط في تقويم لوحة الإدارة.
- **القديم** (`src/lib/appointment-slots.ts`): يستخدمه نموذج الحجز العام (`AppointmentForm`, `src/app/(public)/[lang]/appointment/actions.ts`)، بقيم **ثابتة مُشفَّرة في الكود**، لا يقرأ من `ClinicSettings` إطلاقًا.

**جديد بعد هذه الجولة**: نموذج الحجز العام أصبح ثنائي اللغة عبر `getAppointmentFormSchema(dict)` في `src/lib/validations/appointment.ts` — أي حل لهذا البند (توحيد المصدرين، أو إضافة فحص ساعات دوام حقيقي لمنطق الحجز العام) سيحتاج **إضافة أي رسالة خطأ/تسمية جديدة إلى كلا القاموسين `src/lib/i18n/dictionaries/ar.ts` و`en.ts` معًا** (النوع `Dictionary` المُشتَق من العربي سيفشل البناء تلقائيًا إن نسيت أحدهما — هذا مفيد كخط دفاع، ليس عائقًا).

**قبل الكتابة، اسأل المستخدم**:
- هل يجب أن يمنع نموذج الحجز العام فعليًا الحجز خارج ساعات الدوام/في الأيام المغلقة، أم يكفي تحديث العرض النصي فقط؟
- هل هناك حالات استثنائية حالية (حجوزات خارج الدوام مقبولة تاريخيًا) يجب مراعاتها؟

### مهام لاحقة (الترتيب الكامل في `PROJECT_STATUS.md` → "خارطة الطريق المتبقية"):
2. صفحة "تغيير كلمة المرور" الذاتية للموظف.
3. تنزيل PDF مباشر للفواتير والتقارير.
4. ربط الفواتير بالتأمين الصحي.
5. قوالب بريد إلكتروني ديناميكية (لا بنية بريد أصلًا — راجع القيد رقم 6 في `PROJECT_STATUS.md`؛ إن بُنيت مستقبلًا، يجب أن تراعي لغة الحجز أيضًا).
6. إعادة النظر في نظام الأرقام (فقط بتأكيد صريح من المستخدم).
7. دمج خطط العلاج بصريًا داخل التقويم.
8. إشعارات لكل مستخدم بدل حالة قراءة عامة.
9. ربط إدارة الأطباء بمركز الإشعارات (إن رغبت العيادة لاحقًا).

---

## 3) ملاحظات تنفيذية مهمة (Important Implementation Notes)

- **⚠️ ابحث دائمًا عن ميزات موجودة مسبقًا قبل البناء** — درس متكرر من جولات سابقة. لهذه الجولة القادمة: اقرأ `src/lib/appointment-slots.ts` و`src/lib/calendar-working-hours.ts` كاملين قبل أي تعديل.
- **مسارات import مطلقة و`src/app/`**: أي نقل/إعادة تسمية مستقبلية لمجلد داخل `src/app/` يتطلب `grep -rn "@/app/<الاسم_القديم>" src/` شاملًا **قبل** اعتبار النقل منتهيًا — راجع القسم 1 أعلاه للتفاصيل الكاملة لما حدث هذه الجولة.
- **`server-only` لا يعمل في سكربتات `tsx` مستقلة** — أي محاولة اختبار مباشر لملف يستورد `server-only` (Server Actions، `get-dictionary.ts`، `create-notification.ts`...) تفشل إلا بتشغيل السكربت عبر `npx tsx --conditions=react-server script.ts`. هذا نمط اختبار جديد مفيد لأي جلسة تحتاج استدعاء Server Action مباشرة من سكربت خارج المتصفح.
- **`react-hooks/set-state-in-effect`, `react-hooks/immutability`, `react-hooks/incompatible-library`**: عائلة قواعد React Compiler لا تزال تظهر في كل مكوّن عميل جديد تقريبًا. هذه الجولة أضافت مثالًا جديدًا لـ`immutability`: تعديل `document.cookie` مباشرة داخل جسم مكوّن — الحل: انقل التعديل إلى دالة عادية مُعرَّفة **خارج** المكوّن (`src/components/layout/language-switcher.tsx` مرجع جاهز لهذا النمط).
- **أي نص جديد يُضاف لصفحة الموقع العام يجب أن يُضاف لكلا القاموسين** `src/lib/i18n/dictionaries/ar.ts`/`en.ts` معًا — النوع `Dictionary` (مُشتَق تلقائيًا من العربي عبر `typeof ar`) سيفشل `tsc`/`build` فورًا إن نسيت أحدهما في الطرف الإنجليزي (`en.ts` يستخدم `satisfies Dictionary`).
- **لا تُضِف `as const` على كائن قاموس اللغة العربية** — يُضيّق كل قيمة نصية إلى نوعها الحرفي (Literal Type) بدل `string`، فيفشل `en.ts` بمطابقة كل نص مختلف. هذا حدث فعليًا هذه الجولة، أُصلح بإزالة `as const`.
- **`BookingDialogProvider`/`AppointmentForm` الآن يعتمدان على `usePublicI18n()`** — أي مكوّن/صفحة تُركِّبهما مستقبلًا (خارج `(public)/[lang]/layout.tsx`) يجب أن تكون فعليًا داخل شجرة `PublicI18nProvider`، وإلا ستفشل بنفس الخطأ الذي ظهر لـ`/login` هذه الجولة.

---

## 4) أشياء يجب عدم تغييرها إطلاقًا (Things That Must NOT Be Changed)

- **`ActivityLog` ونظامه بالكامل، `Notification` ومركز الإشعارات بالكامل** — لم يُمسّا.
- **صلاحيات `permissions.ts`** لكل الأدوار — لم تُمسّ هذه الجولة.
- **`src/app/(admin)/**` بكل محتواه** — نُقل فعليًا لكن **سلوكه/محتواه لم يتغيّر حرفًا واحدًا** (فقط مسارات import أُصلحت لتطابق الموقع الجديد على القرص).
- **`working-hours.ts`**: `DAY_LABELS`/`formatWorkingHoursSummary` الافتراضي (بلا `locale`) يبقى بالضبط كما كان لصالح `working-hours-editor.tsx` الإداري — أي تعديل إضافي يجب أن يكون بنفس نمط المعامل الاختياري الإضافي، لا كسر التوقيع الحالي.
- **بيانات `ClinicSettings` (اسم العيادة، العنوان، الهاتف...) لا تُترجَم ولا تحصل على حقول إنجليزية** بدون نقاش صريح جديد — هذا قرار مؤكَّد من المستخدم لهذه الجولة، وليس افتراضًا يمكن الرجوع عنه بصمت.
- **لا تُضِف `next-intl` أو أي مكتبة i18n خارجية** بدون نقاش — القرار الحالي (قاموس يدوي + route groups) مقصود ومُوثَّق ويعمل بصفر تبعيات إضافية.
- **لا تحذف حساب موظف الاستقبال التجريبي** من تلقاء نفسك.

---

## 5) قائمة تحقق للجلسة القادمة (Checklist for Next Session)

- [ ] اقرأ `PROJECT_STATUS.md` كاملًا أولًا للسياق التاريخي الكامل.
- [ ] **ابحث في الكود (`grep`/`find`) عن أي تنفيذ موجود مسبقًا** قبل افتراض عدم وجوده.
- [ ] تحقق أن خادم التطوير يعمل فعليًا (تم إيقافه عمدًا في نهاية هذه الجلسة)؛ شغّله بـ`npm run dev`.
- [ ] إن ظهر خطأ `Cannot find module '.../page.js'` من `.next/types/` عند `tsc`/`build`، **احذف `.next` وأعد المحاولة أولًا**.
- [ ] شغّل `npx prisma migrate status` للتأكد أن قاعدة البيانات ما زالت متزامنة (11 migration).
- [ ] **اسأل المستخدم عن نمط ربط ساعات الدوام بالحجز العام** (منع فعلي مقابل عرض نصي فقط) قبل كتابة أي كود.
- [ ] أي نص جديد على الموقع العام → أضِفه لكلا `ar.ts`/`en.ts` معًا، ودَع `tsc` يتحقق من التطابق.
- [ ] بعد أي تعديل: `npx tsc --noEmit` ثم `npx eslint` على الملفات المتأثرة ثم `npm run build`.
- [ ] **اختبر أي مكوّن عميل جديد ذي حالة تفاعلية ضد قواعد `react-hooks/*`** — توقَّع ظهورها، أصلحها بإعادة الهيكلة لا بالتعطيل.
- [ ] احذف أي سكربت اختبار مؤقت (`scripts/_tmp-*.ts`) وأي حساب/بيانات تجريبية فور انتهاء الاختبار.
- [ ] حدّث `PROJECT_STATUS.md` و`NEXT_TASK.md` في نهاية الجلسة بنفس الأسلوب.
